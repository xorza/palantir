debugoverlay for colliding widgetids

there is a problem with delayed tooltip, i guess if no events are coming - it will never get an update to actually show, seems like we need to
have a delayed redraw request system
