image

local animation that dont need relayout

checkbox

user-provided hashses for meshes
and aabb

Ui::default(); crate only or test only
Frontend

    pub(crate) bbox: Rect,
    pub(crate) origin: glam::Vec2, - why both
