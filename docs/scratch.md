- showcase agent testing

scissor rounderer corner clipping

line with two or one color

extract tree shapestorage

helper method for self.width <= 0.0 || self.width.is_nan() || approx_zero(self.width)
laso negative color alpha
