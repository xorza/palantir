//! The harness's own protocol: how many passes it runs, what it addresses,
//! and what it must not let a test get wrong.

mod addressing;
mod clock;
mod fixtures;
mod input;
mod passes;
mod support;
mod surface;
