# Palantir crate review

Findings only. No fixes are proposed here. When you address an item, delete it
from this file. When a whole group is empty, delete the heading.

Anchors are relative to the `palantir` crate root. Tests, benches, and the
gated `internals` / `test_support` modules were not reviewed.

Groups are sorted by severity and benefit. Items inside a group are sorted the
same way.

## Full-tree or per-item work is repeated on the interactive path

- [ ] `src/layout/pass.rs:290-301`, `src/layout/intrinsic/mod.rs:346-374`, `src/layout/engine.rs:121-124` — `LayoutPass::measure` issues `intrinsic(node, X, MinContent)` and `intrinsic(node, Y, MinContent)` independently. For a text leaf each reaches `intrinsic::leaf`, which calls `engine.text.root(..)` per run and then reads `axis.main(..)` off a `Size` that carries both axes. Every text leaf does two `TextSystem::root` lookups for one `TextRoot`, plus its `measure`; `cached_intrinsic` excludes leaves from cross-frame reuse, so this repeats on every changed frame.
- [ ] `src/host/winit/gpu/mod.rs:130`, `src/host/core.rs:48` — `DeviceRequirements::max_texture_dim(&device)` is computed once for `SurfaceManager.max_texture_dim` and again inside `HostCore::new` for `TextureLimit` and `Frontend`, from the same device in the same startup sequence.
- [ ] `src/host/winit/runtime.rs:43,95,176-182`, `src/ui/mod.rs:728` — `sync_diagnostics` runs every `about_to_wait`, borrows the `RefCell`, and diffs against a mirrored `observed_overlay` to learn that some `Ui::set_debug_overlay` call happened; the writer side is the only mutator.
- [ ] `src/widgets/splitter/mod.rs:152-156` vs `src/widgets/tooltip/mod.rs:164-172,227-229`, `src/widgets/combo_box/mod.rs:156-161,191-193`, `src/widgets/context_menu/mod.rs:120-129` — `Splitter` calls `ui.state_mut::<SplitterState>(id)` unconditionally every frame, materialising a row for every splitter on its first frame and hashing on every later one, while `Tooltip`, `ComboBox`, and `ContextMenu` probe with `try_state` and write only on a real change, each with a comment saying why.
- [ ] `src/widgets/drag_value/mod.rs:447-449,460` — `show_editing` keeps only `resp.submitted` from the `TextEdit`'s response and returns a fresh `Response::lazy(id, ui)` for the same id — a second `response_for` probe on the edit frame if the caller reads it.

## Frame cost is not uniform, or a hot path allocates

- [ ] `src/renderer/backend/raster_atlas/clock_sweep.rs:57`, `src/renderer/backend/raster_atlas/mod.rs:668-703`, `src/renderer/backend/text/mod.rs:130-153`, `src/renderer/backend/text/encode/encoder.rs:104-108`, `src/renderer/backend/icon/mod.rs:274-298` — a slot is evictable when `last_use < current_frame`, and `current_frame` advances only in `advance_to` at the end of a submit, so at the start of a frame's `prepare_batch` walk every slot drawn last frame is as eligible as a cold one. Under pressure a miss in batch 1 can take a slot batch 3's cached template references; `try_emit_cached` sees the generation mismatch, drops the row, re-extracts and re-rasterizes the run, and that insert evicts another live slot — a cascade inside one frame whose length is the number of new glyphs. The `evict_one` doc claims "anything drawn this frame is skipped outright"; the guarantee is "anything drawn so far this frame". The icon atlas has the same predicate.
- [ ] `src/renderer/backend/text/encode/encoder.rs:289`, `src/text/glyphs/mod.rs:130`, `src/text/cosmic/mod.rs:677-694` vs `src/renderer/backend/icon/mod.rs:61,227` — `rasterize_and_insert` receives a `GlyphImage { data: Vec<u8> }` (swash's `get_image_uncached` allocates per call and `rasterize_glyph` moves it out), so every glyph miss heap-allocates and frees once, on the path the atlas docs describe as re-rasterizing thousands of glyphs a frame. The icon tenant writes into the backend's retained `staging: Vec<u8>` out-param.
- [ ] `src/layout/cache/mod.rs:174-189,275-421`, `src/layout/counters.rs:159-166` — on a `cache_rebuild` frame `capture_tree` copies every column of every node, runs the reverse text-bounds walk, and pushes one descriptor per non-leaf; `end_frame` then rebuilds the whole `WidgetIdMap` in `refresh_snapshots` whenever the descriptor identity changed. A frame that adds or removes one widget pays a full map rebuild the neighbouring frames do not.
- [ ] `src/text/cosmic/mod.rs:73,133-137,577-607` — the `ExpiryWheel` bounds the scan to what expires, not the drop: every buffer inserted on frame F under the protected window comes due together on F+121, so a page switch that shapes 500 runs frees them all on one later frame, and past `RECYCLE_POOL_CAP = 128` each `Buffer` drop frees cosmic's line/shape/layout allocations rather than pooling them.
- [ ] `src/text/cosmic/mod.rs:243-248,823-829` — the doc says the truncation miss path "keeps that path free of `String` allocs", but every `Buffer::set_text` goes through cosmic's `set_rich_text_impl`, which collects the spans into a fresh `(String, Vec<_>)`. Each miss and each back-off retry allocates at least twice regardless of the recycle pool.
- [ ] `src/scene/record_store/recorded_gradients.rs:28,44,50` — `ids: FxHashMap<u64, GradientId>` is cleared and re-inserted per gradient per frame. Capacity is retained so it is not an allocation, but it is the per-frame map rebuild `CLAUDE.md` names as a regression.
- [ ] `src/text/cosmic/geometry.rs:100-108`, `src/text/wrap/mod.rs:122-125` — `collect_break_offsets` reruns `unicode_linebreak::linebreaks` over each layout run's text to find UAX #14 boundaries that cosmic already computed while shaping but does not expose. The cost is documented as 8x-25x the rest of the measurement; the scan plus `binary_search` per glyph is a second pass over data the shaper had.
- [ ] `src/host/winit/window.rs:208-211` — `Timeout | Validation` return `true` (repaint immediately). A persistent `Validation` failure builds a full CPU draw list and re-requests a redraw on every loop tick with no back-off, unlike the `Occluded` arm which yields.
- [ ] `src/icons/icon_registry/mod.rs:289-303` — `IconRegistry::sets` allocates a `Vec<(IconSetId, Rc<IconAtlas>)>` (tuple items, an `Rc` clone per set) for its one caller `IconBackend::prewarm`; documented as cold, but it is the one place the registry hands out tuples.
- [ ] `src/scene/tree/paint_anims/mod.rs:242,247-255,268-277`, `src/scene/tree/mod.rs:175-179`, `src/scene/damage/mod.rs:423` — `entries` and `shape_indices` are parallel `Vec`s; `push_entry` pushes both, `Tree::post_record` debug-asserts equal lengths, and the cursor indexes both by one `next`. `PaintAnimEntry.node_idx: u32` is a `NodeId` stored raw and cast back with `as usize`.

## Device acquisition and the host lifecycle are written once per host

- [ ] `src/host/headless_gpu.rs:34-75` vs `src/host/winit/gpu/mod.rs:55-128`, `src/host/bench_gpu.rs:65`, `src/host/test_gpu.rs:34` — `HeadlessGpu::new` and `GpuInit::new` each perform the same five steps: `enabled_backend_features().is_empty()` check, instance from env, `request_adapter` with the identical four-field options, `DeviceRequirements::negotiate`, `request_device`. Neither calls the other. They have drifted: the headless path uses `InstanceDescriptor::new_without_display_handle_from_env()` (which also applies `backend_options.with_env()`) while the winit path hand-applies only `backends.with_env()` and `flags.with_env()`; winit uses `MemoryHints::Performance`, headless `default()`; winit logs the adapter, headless does not; one blocks with `pollster::block_on(..)`, the other with `FutureExt::block_on()`.
- [ ] `src/host/error.rs:54-63,72-74` vs `src/host/winit/error.rs:52-59,91,118` — two parallel error enums with the same four variants under different names and messages (`NoBackend` vs `NoGpuBackend`, "failed to find a graphics adapter" vs "… a compatible graphics adapter", `Requirements`, `RequestDevice`). `HeadlessGpuError::Requirements` prefixes its `Display` while `WinitHostError::Requirements` forwards the source's `Display` verbatim and returns it from `source()`, so a chain-printing reporter emits the identical sentence twice for the winit variant only.
- [ ] `src/host/winit/gpu/mod.rs:112-114`, `src/host/bench_gpu.rs:56-58`, `src/renderer/backend/mod.rs:286-295` — the timing-feature triple `TIMESTAMP_QUERY | TIMESTAMP_QUERY_INSIDE_PASSES | PIPELINE_STATISTICS_QUERY` is spelled inline twice, and `WgpuBackend::new` tests the same three bits individually; no shared constant names the set the `collect_gpu_stats` opt-in stands for.
- [ ] `src/host/device_requirements.rs:50-81` vs `:104-121` — `against` derives limits with `check_limits_with_fail_fn` over the whole `Limits` after raising `max_immediate_size`, while `met_by` re-implements the contract by hand: the same `FEATURES` containment test and a single explicit `max_immediate_size < IMMEDIATE_SIZE`. Any limit added to `against` has to be mirrored in `met_by`, and the `Limit { name: "max_immediate_size" }` literal is spelled independently of the name `check_limits_with_fail_fn` reports.
- [ ] `src/host/winit/gpu/mod.rs:260-285` vs `:287-297` — `negotiate_present_mode`'s fallback arms (`Fifo | FifoRelaxed → AutoVsync`, `Immediate | Mailbox → AutoNoVsync`) encode the same two-way partition of `wgpu::PresentMode` that `vsync_of` and `present_mode` already define. Adding a present mode has to be classified in two places.
- [ ] `src/host/offscreen.rs:66-76,138-146,161-170` vs `src/host/window_driver/mod.rs:258-264,305-313` — `OffscreenHostBuilder` re-declares `clock` and `pixel_snap` with the defaults `WindowDriverBuilder` already declares, then copies each across. The two disagree on shape: `OffscreenHostBuilder::clock(impl Clock + 'static)` boxes internally, `WindowDriverBuilder::clock(Box<dyn Clock>)` takes the box.
- [ ] `src/host/winit/runtime.rs:83,218`, `src/host/winit/config.rs:12-26`, `src/host/window_driver/mod.rs:315-336` — the winit host builds every driver with `core.driver(token).build()` and never calls `.pixel_snap(..)`; `WinitHostConfig` has no field for it. `pixel_snap` is configurable on the offscreen host only, though `WindowDriver::display` is documented as the place the setting reaches a frame for both hosts.
- [ ] `src/window/window_frame_state.rs:7-11`, `src/window/window_geometry.rs:13-24`, `src/window/window_config.rs:14-53` — the pair (physical position, maximized) lives in `WindowFrameState { position, maximized }`, `WindowGeometry { outer_position, maximized }`, and `WindowConfig { position, maximized }`; `inner_size` is `UVec2` on one and `Option<UVec2>` on another. The documented persist/restore round trip is a manual field-by-field copy across two vocabularies.
- [ ] `src/host/winit/window_set.rs:22-24,71-75` vs `src/window/window_directory.rs:10-12,19-31`, `src/host/core.rs:66-69,84`, `src/host/winit/runtime.rs:212-215` — `WindowSet` (a `Vec<Window>` scanned by token) and `WindowDirectory` (`Rc<RefCell<Vec<WindowToken>>>` scanned by token) both hold the live tokens; `HostCore::driver` / `retire` keep the second in step by hand. A duplicate token is guarded three times at three strengths (warn-and-ignore, release-assert, release-assert); only the first can fire. `set_live(token, live: bool)` is one method whose `bool` selects insert vs remove, and both callers pass a literal. `HostCore::driver` marks the token live when the builder is created, not when `build()` runs, so a dropped builder leaves a phantom live token.
- [ ] `src/host/winit/window.rs:197-207` vs `:247-253`, `src/host/window_driver/mod.rs:384-386` — `set_vsync`'s doc states that `surface.configure` happens only through the `note_target` gate. `present` calls `surfaces.configure(..)` directly in the `Suboptimal` and `Outdated | Lost` arms without touching `output_valid` / `backbuffer_fresh`. It is safe only because `finish_cpu_frame` already cleared `output_valid` — a coupling nothing states.
- [ ] `src/host/winit/gpu/mod.rs:24-25,46-51,131-138,168-174`, `src/renderer/backend/mod.rs:191-192` — `GpuInit` returns `device` and `queue` alongside a `SurfaceManager` that already holds clones of both only to service `configure` and `present`, while `WgpuBackend` on the same `HostCore` holds the same handles privately.
- [ ] `src/host/winit/gpu/mod.rs:241-242` vs `src/host/winit/mod.rs:411-412` — `clamp(1, max_texture_dim)` on width and height is spelled in `build_surface_config` and again in the `Resized` handler.
- [ ] `src/host/window_driver/mod.rs:517-533` vs `:536-564` — the `Direct` and `ViaBackbuffer` arms construct an identical `Submission { owner, targets, payloads, buffer, plan, debug_overlay }` differing only in `backbuffer: None` vs `Some(..)` and `backbuffer_fresh`.
- [ ] `src/host/winit/config.rs:12-26` + `src/host/winit/mod.rs:162-212` vs `src/host/offscreen.rs:66-111` — the winit host exposes a `pub` config struct with `pub` fields and a builder whose setters write those fields; the offscreen builder keeps private fields and setters only. `WinitHostBuilder::present_mode` and `::vsync` are two setters writing the same `config.present_mode` in two vocabularies.
- [ ] `src/host/winit/config.rs:33`, `src/host/test_gpu.rs:27-28,35`, `src/host/bench_gpu.rs:65` — the windowed default is `PowerPreference::LowPower`, while every headless caller asks for `HighPerformance` and `test_gpu.rs` describes `HighPerformance` as "the one a windowed host is normally configured with".
- [ ] `src/host/winit/input/mod.rs:62,198-202`, `src/ui/harness/mod.rs:114-118`, `src/widgets/text_edit/input.rs:246` — `InputEvent::Text` is emitted only from `WindowEvent::Ime(Ime::Commit(..))`. Nothing calls `Window::set_ime_allowed`, and the harness says so ("dead in production today"). `KeyEvent::text` is never read; typed text reaches `TextEdit` solely through `KeyDown { key: Key::Char(c) }`. `emit_text_chunks`, the `Ime` arm and its tests exist for a path no production build hits.
- [ ] `src/host/clock.rs:22-41`, `src/host/window_driver/mod.rs:92`, `src/host/offscreen.rs:74` — `Clock` is a trait with one production implementor; `FixedClock` is used only by tests. The default no-op `skip` / `deadline` bodies exist so the test clock need not implement them, and every driver pays `Box<dyn Clock>` for that.
- [ ] `src/host/offscreen.rs:181-183` vs `src/ui/mod.rs:728-730` — `OffscreenHost::set_debug_overlay` writes `shared.resources.diagnostics.overlay` directly; `OffscreenHost::ui()` already exposes the `Ui` whose `set_debug_overlay` writes the same cell.
- [ ] `src/host/winit/error.rs:45-51`, `src/host/winit/runtime.rs:216-217` — `CreateWindow` carries the `WindowToken` it failed for; `CreateSurface`, raised per window on the same `spawn_window` path, carries none.

## One fact is held by two owners

- [ ] `src/scene/cascade/mod.rs:136-139`, `src/scene/damage/node_snapshot.rs:50-59`, `src/scene/cascade/engine.rs:221-223,462`, `src/scene/damage/walk.rs:165,166,264,375` — last frame's `Tree.rollups.subtree[i]` is snapshotted twice on independent lifetimes: `LayerCascade::subtree_hashes` (per node) and `NodeSnapshot::subtree_hash` (per widget). `NodeSnapshot::cascade_input` likewise copies `LayerCascade::cascade_inputs[i]`. Both engines compare the same current column against their own retained copy of the same previous value.
- [ ] `src/scene/cascade/engine.rs:349-353,535-541`, `src/scene/damage/walk.rs:126-152,183-202,359-363` — `CascadeEngine::run_tree` and `LayerWalk::run` are two hand-written pre-order walks over the same `records` column, each with its own ancestor stack keyed on `subtree_end` (`Frame { subtree_end }` retired by `pop_if` vs `ParentFrame { end, key }` retired by a `while` loop). The damage stack exists only to recover each node's parent `WidgetId`, which the cascade stack already has in hand and does not record. `on_subtree_moved` runs the same retire/push pair per node a second time.
- [ ] `src/scene/seen_ids/mod.rs:92-96`, `src/scene/cascade/mod.rs:213`, `src/scene/cascade/engine.rs:236` — `SeenIds.prev` (last painted frame's `WidgetId → Endpoint`) and `Cascade.by_id` (`clone_from(&ids.curr)` on every full rebuild) retain the same id→endpoint entries; the `prev` doc says only its keys are read. Two O(N) maps plus one O(N) copy per rebuild for one relation, at two refresh cadences.
- [ ] `src/scene/damage/mod.rs:206-214` → `src/renderer/render_plan.rs:22-30,49-56` → `src/renderer/backend/viewport.rs:17-20,78-94`, `src/renderer/backend/mod.rs:452` — `Damage { Skip, Full, Partial(..) }` maps 1:1 onto `RenderKind { Full, Partial { damage } }` which maps 1:1 onto `RepaintScissors { Full, Partial(..) }`. Each hop restates the Full/Partial split with a new payload. `submit` re-derives `is_partial` from the third enum rather than reading `plan.kind`, which it already holds.
- [ ] `src/renderer/backend/mod.rs:172-178,433-434,452-453,747` — `UploadPlan` carries `is_partial`, `dim_undamaged`, and `use_stencil`, all derivable from what `Submission` already holds (`plan.kind`, `debug_overlay.dim_undamaged`, `targets.stencil.is_some()`). `run_main_pass` re-derives `use_stencil` from `stencil_view.is_some()` a second time. Only `clear` is new information.
- [ ] `src/renderer/frontend/composer/session.rs:72,136,154,182,292,886,921-922,1208`, `src/renderer/render_buffer/mod.rs:140-145,185-186`, `src/renderer/frontend/composer/mod.rs:182-185`, `src/renderer/backend/mod.rs:439,506` — `ComposeSession.display` and `RenderBuffer.{viewport_phys, viewport_phys_f, scale}` are the same frame values stamped twice by `Composer::begin`. Handlers read `self.display.physical`, but `fold_into_clear` reads `self.out.viewport_phys_f` and `discard_composed` hands `self.out.viewport_phys` where `begin` handed `display.physical`. `viewport_phys_f` is `viewport_phys.as_vec2()` stored beside it, and backend readers pick one or the other by convenience.
- [ ] `src/ui/frame_cycle.rs:95`, `src/input/input_state/mod.rs:136,469` — `InputState::frame_time` is a copy of `FrameRuntime::time` refreshed once per frame by the driver; its only reader is `begin_press` timing. A mirrored clock kept in sync by a third party.
- [ ] `src/ui/frame_runtime/mod.rs:48-111`, `src/ui/mod.rs:415,485,1236`, `src/ui/frame_cycle.rs:96-102,107,220,275-276,375` — every `FrameRuntime` field is `pub(crate)`. `relayout_requested` / `repaint_requested` are set by `Ui` and cleared by `FrameCycle`; `prev_stamp` / `prev_cascade_fp` are written from `frame_cycle.rs`; meanwhile `schedule_wake`, `note_processing`, `advance_clock`, `take_frame_plan` are methods on the same struct. `FrameCycle::run` reads `self.ui.input.signal_since_last_frame` and `self.ui.window_frame.close_requested` as fields though `Ui::close_requested()` exists — the pattern the `Ui` doc at `mod.rs:113-116` objects to.
- [ ] `src/renderer/backend/mod.rs:228-237,286,296`, `src/renderer/backend/gpu_timings.rs:137` — `WgpuBackend.pass_stats` and `GpuTimings.sink` are two clones of the same `GpuPassStats` handle. The comment justifies the backend field by the CPU-time publish; it does not explain why `GpuTimings` needs its own copy rather than being handed the backend's at `after_submit`.
- [ ] `src/scene/cascade/mod.rs:140-146,251-258`, `src/scene/cascade/engine.rs:467` — `LayerCascade::subtree_ends: Vec<u32>` is a per-node copy of `Tree.records.subtree_end()` with the grid flag stripped, written for every node on every full rebuild. Its only reader is `Cascade::is_within`. This is the third retained copy of tree structure beside the two hash snapshots.
- [ ] `src/widgets/context_menu/mod.rs:71,79,137,146-147`, `src/widgets/popup/mod.rs:133,167,185-188`, `src/widgets/combo_box/mod.rs:175` — `ContextMenu` keeps its own `chrome: Option<Background>` beside the `Popup` it wraps, which has its own; `show` copies it across. The popup is built with a placeholder `Popup::anchored_to(Vec2::ZERO)` (whose `#[track_caller]` id is minted inside `for_id`, which is not `#[track_caller]`) and patched by the `pub(crate)` one-caller `Popup::anchored_at` and `default_id`. `.click_outside(ClickOutside::Dismiss)` in `ContextMenu` and `ComboBox` is redundant — `Popup::positioned` already defaults to `Dismiss`.
- [ ] `src/widgets/text_edit/view_state.rs:322-343` — `ViewUpdateInput` carries `response_rect`, `ctx`, `caret_width`, `content_size`, `caret_pos`, `block_offset`, which are all fields of the `TextGeometry` / `TextLayout` the caller already holds, re-listed one at a time; thirteen fields for a call whose only new information is the wheel, the clock, and four flags.
- [ ] `src/widgets/text_edit/view_state.rs:304,340` vs `src/widgets/text_edit/mod.rs:350,398,405-406,473-474` — `ViewState` owns `prev_focused`, yet `gained_focus` is computed outside from a snapshot and passed in; the `!metrics_valid` early return writes `state.view.prev_focused` by hand, a second writer of a field `ViewState::update` is supposed to own.
- [ ] `src/widgets/text_edit/input.rs:23-33`, `src/widgets/text_edit/mod.rs:566-577,585-605`, `src/widgets/text_edit/view_state.rs:346` — one frame's outcome crosses three structs, renaming as it goes: `InputResult.blur` → `EditSignals.cancelled` → `TextEditResponse.cancelled`; `InputResult.edited` → `EditSignals.changed` → `TextEditResponse.changed`. `ViewUpdate.scroll` hands back a copy of `state.view.scroll` the caller already owns.
- [ ] `src/layout/scrollbars/mod.rs:55-74,86` vs `src/layout/types/layout_mode.rs:181-191` — `ScrollbarsDef.pan` repeats the pan mask the sibling `Scroll` node already carries in `ScrollSpec`, and `hash_visual` hand-packs it into a `u8` while `ScrollSpec` packs the same bits into a `u16`.
- [ ] `src/widgets/scroll/mod.rs:46,64-70,385-393` — `ScrollGeometry.content_margin` is a copy of `Scroll.content_margin` carried so `bounds()` can build a `ScrollBounds`; the only `bounds()` call site has `self` in hand.
- [ ] `src/renderer/frontend/composer/mod.rs:139`, `src/renderer/frontend/composer/session.rs:1036-1041,1065-1071` — `OpenBatch.texts_start` is always the previous `TextBatch`'s `texts.start + texts.len` (or 0); the `debug_assert!` in `close_batch` pins exactly that. `open_batch` initialises `last_group: 0` and overwrites it on the next line.
- [ ] `src/renderer/backend/text/encode/encoder.rs:49-50,139-154,164-169` — `starved_this_frame` / `starved_reported` together represent {idle, starved-and-reported, reported-last-frame-not-yet-this}; the fourth combination is only transient inside `note_atlas_starved`.
- [ ] `src/layout/types/layout_mode.rs:11-22`, `src/layout/driver.rs:131-134,170-177` — `HStack / VStack / WrapHStack / WrapVStack` are four variants for two drivers × `Axis`; `DriverOp::dispatch` immediately maps them back to `(Stack, Axis)` / `(WrapStack, Axis)`, and `ScrollChildLayout::Flow(Axis)` already shows the payload form. `Grid(id)`, `Scroll(spec)`, `Scrollbars(id)` carry payloads; the stacks alone do not.
- [ ] `src/renderer/frontend/encoder/layer_ctx.rs:436,453,460-482,503-507`, `src/scene/tree/mod.rs:603-608` — `let clip = mode.is_clip()` is tested at 453, and the `match mode` inside that branch still carries an unreachable `ClipMode::None => {}` arm. `transform_of` already `.filter(|t| !t.is_identity())`; the encoder maps through `anchored_at` (which cannot turn a non-identity into an identity) and filters identity a second time.
- [ ] `src/renderer/frontend/composer/session.rs:923-926`, `src/renderer/frontend/composer/clip_stack.rs:38-48` — `fold_into_clear` rejects on `clip.scissor().is_some() || clip.chain().len != 0`. A non-empty chain implies a frame on the stack, which implies `Some` scissor, so the second test is implied by the first.
- [ ] `src/input/input_state/mod.rs:685-696,745-755` — in `snapshot_frame_quiescent`, `hovered / scroll_target / pinch_target .is_none()` are implied by `pointer_pos.is_none()`: `refresh_pointer_targets` is the only writer and clears all three whenever `pointer_pos` is `None`. Corollary: the `response_for` fast path fires only while the pointer is off-surface, not on an idle frame with the pointer resting on inert surface.
- [ ] `src/ui/frame_runtime/mod.rs:78-79,114-130,132,151`, `src/ui/frame_cycle.rs:371` — `dbg_cascade_ran`, `note_cascade_ran(#[allow(unused_variables)] ran)`, and `cascade_ran` are test-only state routed through a production method. `pub(super) const MAX_DT: f32 = MAX_ANIM_DT;` is a second name for one constant with a single use.
- [ ] `src/ui/frame_cycle.rs:128` vs `:398`, `src/text/system.rs:132-136` — the shaper's frame clock is ticked through two objects in one function: `ui.resources.text.tick_frame()` on the PaintOnly arm, and `engines.layout.text.end_frame(removed)` — whose first statement is `self.shaper.tick_frame()` — in `finalize_frame`.
- [ ] `src/renderer/backend/icon/mod.rs:73-76,275,298` vs `src/renderer/backend/text/mod.rs:239` — the two `RasterAtlas` tenants age on different clocks: text advances to `shaper.frame()`, icons count their own submits in `IconBackend.frame`, although `WgpuBackend` holds the shaper the icon side could read.
- [ ] `src/scene/cascade/engine.rs:68-76,354-366` — `Inherited` is a two-field struct whose only construction and only destructure sit in the same `match` expression; it exists to name the pair coming out of `self.stack.last()`.
- [ ] `src/scene/cascade/paint_rect.rs:127,214` — `has_children` is threaded in through `PaintRectCtx` while `has_shapes` is read locally from `records.shape_span()`, though the ctx doc argues fields earn a place only when the walk already holds them; both are one indexed load off the same records.

## Two implementations exist for one role

- [ ] `src/renderer/backend/text/mod.rs:86-106,120-125,156-158,178-198,219-226` vs `src/renderer/backend/icon/mod.rs:123-141,188-193,214-215,250-269,274-301`, `src/renderer/backend/text/encode/encoder.rs:39`, `src/renderer/backend/icon/mod.rs:65` — `RasterAtlas::draw_span` was factored out because the draws were byte-identical, but everything around it is still written twice: both backends own a `shader`, a `vbuf: DynamicBuffer<RasterQuad>`, an `instances: Vec<RasterQuad>`, a `ranges: Vec<Span>`, the `debug_assert_eq!(batch_idx, self.ranges.len())` contract, the `Span::new(start, end - start)` push, the `ranges.get(batch_idx).expect(...)` lookup, the `vbuf.upload_instances` + `atlas.flush_pending_uploads` pair, and a `StencilVariant::build` call identical except for three label strings. Ownership is asymmetric: text keeps `instances` on `TextEncoder`, icon on `IconBackend`.
- [ ] `src/renderer/backend/icon/mod.rs:218-246` vs `src/renderer/backend/text/encode/encoder.rs:281-321` — `IconBackend::slot` and `rasterize_and_insert` both do rasterize → kind-to-`ContentType` → `PackedMetadata::new` → `is_empty ? insert_unallocated : insert`, written out twice. They disagree on a `PackedMetadata::new` failure: the icon side returns `None` and retries next frame, the text side warns and files an `EMPTY` unallocated slot. `TextEncoder` has an edge-triggered `tracing::warn!` for a full atlas; `IconBackend::slot` folds "could not rasterize" and "atlas full" into one silent `None`.
- [ ] `src/renderer/backend/raster_atlas/content_type.rs:13`, `src/text/render.rs:47`, `src/icons/icon_rasterizer/mod.rs:16`, `src/renderer/backend/text/encode/encoder.rs:292-295`, `src/renderer/backend/icon/mod.rs:228-231` — `ContentType`, `GlyphImageKind`, and `IconRasterKind` are three two-variant `Mask | Color` enums with identical meaning; each tenant translates its own into `ContentType` with a two-arm match. `IconRasterKind` carries the same doc text as `ContentType`.
- [ ] `src/renderer/backend/instance_pipeline.rs:28-91`, `src/renderer/backend/mod.rs:134,387-388,984,997`, `src/renderer/backend/format_pipelines.rs:17,82-89`, `src/renderer/backend/mesh_pipeline.rs:59-61`, `src/renderer/backend/image_pipeline/mod.rs:107-111` — `InstancePipeline` has four impls and zero generic uses; the only non-impl mentions are `use` lines bringing the methods into scope. Its four GATs exist to let two impls write `()`, and every call site then spells the `()` out. Text and icon implement the same five-step shape outside the trait with a different signature (`build_variants(device, format)` with no `layouts`; `render_batch(..)` doing bind + push + draw in one), so `render_groups` has two shapes per step (`mod.rs:959-971,1007-1014`), and `FormatPipelines::new` mixes three `build_variants` argument shapes. The icon arm ignores the `items` it just computed (`mod.rs:979`).
- [ ] `src/renderer/backend/raster_atlas/mod.rs:531,767-795`, `src/renderer/backend/text/encode/cache.rs:144-161,223`, `src/text/cosmic/mod.rs:583-606` — all three owners of an `ExpiryWheel` write the same retire body: look the key up, `None` if gone, re-read the entry's `last_use` / `keep_until`, `Some(deadline)` if live, else remove and release. The wheel doc calls this "the whole owner-side protocol in one call", yet each owner spells it. The deadline arithmetic `X + KEEP_FRAMES + 1` is duplicated within each owner, and the two retire sites already diverge (`get` + `remove` vs `Entry`).
- [ ] `src/renderer/gradient_atlas/mod.rs:117-124,318,339` vs `src/renderer/backend/raster_atlas/clock_sweep.rs:57`; `gradient_atlas/mod.rs:117` vs `raster_atlas/mod.rs:158`; `gradient_atlas/counters.rs:61` vs `raster_atlas/counters.rs:26`; `src/renderer/backend/gpu_gradient_atlas.rs:141-151` vs `raster_atlas/mod.rs:741-752` — both are grow-by-doubling caches under a ceiling with a key→index map, a per-slot key, an "exempt this frame" stamp, and eviction/growth counters. The exemption stamp is an `epoch` per `flush` on one and `last_use` vs a frame clock on the other; the reverse index is `RowSlot.key: Option<K>` vs a parallel `slot_keys: Vec<K>`; the growth counter is `growths` vs `grows`; the rebind step runs eagerly inside `grow` on one and lazily by comparing `texture.height() != rows.total_rows` on the other. The eviction policies are deliberately different and documented; the surrounding vocabulary is not.
- [ ] `src/icons/icon_atlas.rs:59`, `src/icons/icon_set.rs:70` — `IconAtlas` is the third type named "atlas" and is not one: a baked name-sorted icon table plus SVG blob, with no slots or eviction, beside the public handle `IconSet` for the same thing.
- [ ] `src/renderer/backend/quad_pipeline.rs:133-134,146-157,208-231,258-268,364-376,387-397` vs `src/renderer/backend/stencil.rs:44-63`, `src/renderer/backend/stencil_variant.rs:40-41` — `build_mask_pipeline` rebuilds the whole `DepthStencilState` that `Stencil::test_state` claims to be the "sole source of truth" for, differing only in `face` and `write_mask`. Every `FormatPipelines::new` builds three identical `PipelineLayout`s for the quad shader, though `stencil_variant.rs` calls building two identical layouts "pure waste". `bind_clear`, `bind_mask`, and `bind` are the same `set_pipeline` / `set_bind_group` / `set_vertex_buffer` triple.
- [ ] `src/renderer/backend/quad_pipeline.rs:323-328` vs `src/renderer/backend/overlay_pass.rs:72-77`, `src/renderer/backend/dynamic_buffer.rs:29-40` — `clear_buffer` and `dim_buffer` are hand-built single-`Quad` `wgpu::Buffer`s with identical descriptors apart from the label, while `DynamicBuffer::<Quad>::vertex(device, label, 1)` is the crate's constructor for exactly that buffer.
- [ ] `src/renderer/backend/gpu_gradient_atlas.rs:91-100`, `src/renderer/backend/image_pipeline/textures.rs:30-39` — both construct `ClampToEdge ×3, Linear mag/min, Nearest mipmap` samplers field for field, while sharing `texture_binding::layout` / `bind_group` for the surrounding objects.
- [ ] `src/renderer/frontend/payload/brush_source.rs:45-50`, `src/renderer/frontend/payload/resolved_gradient.rs:9-13`, `src/renderer/frontend/payload/draw_quad_payload.rs:81-87,129-146`, `src/renderer/frontend/payload/draw_curve_payload.rs:31-41`, `src/renderer/frontend/encoder/layer_ctx.rs:290-299` — `GpuFillFields {color, kind, lut_row, axis}`, `ResolvedGradient {axis, row, kind}`, `DrawQuadPayload {fill, fill_kind, fill_lut_row, fill_axis}`, and `DrawCurvePayload {color, fill_kind, fill_lut_row}` are four near-duplicate lane bundles with three naming schemes (`row` / `lut_row` / `fill_lut_row`). `rect_impl` destructures a `GpuFillFields` into four locals and re-packs them field by field. `GpuFillFields` exists as a struct yet no payload embeds it.
- [ ] `src/primitives/text_source.rs:14-23`, `src/primitives/interned_text.rs:7-9`, `src/primitives/recorded_text.rs:11-26`, `src/primitives/interned_str.rs:25-28`, `src/scene/record_store/record_payloads.rs:55-59`, `src/layout/text_shape_input.rs:121`, `src/text/shaped_ref.rs:59` — four types for one span. `TextSource` is a `#[repr(transparent)]` newtype over `Span` whose only method slices the interned bytes; `InternedText<'a>` is a newtype over `&str` with a `pub(crate)` field. Each is constructed in exactly one place and read in two. `InternedStr` carries the same `Span` plus an epoch. `RecordedText` has only `Copy` fields but derives `Clone` alone.
- [ ] `src/scene/shapes/hash.rs:264-270` vs `src/scene/shapes/lower.rs:189-196`; `lower.rs:179-206` vs `hash.rs:49-81`; `src/scene/node/bounds_extras.rs:17`, `src/scene/node/panel_extras.rs:18`, `src/scene/node/layout_core.rs:30` — `hash_brush` and `fill_payload` / `fill_tag` inside `background` derive the same two values from a `ShapeBrush` + gradient hash. Chrome is hashed through a function-local `#[padding_struct] ChromeHashBytes` and one `Hasher::pod`, while `compute_record_hash` hashes the same `ShapeStroke` / `Corners` / `LoweredShadow` / `ShapeBrush` field-by-field. Node columns use a third style — `Hash` impls on `BoundsExtras` / `PanelExtras` / `Gaps` / `GridCell` beside a method `LayoutCore::hash_with_flags` that is not a `Hash` impl.
- [ ] `src/scene/shapes/hash.rs:50,53,61,81,243`, `src/scene/shapes/lower.rs:199,206,343`, `src/scene/node/layout_core.rs:35-36`, `src/scene/node/gaps.rs:58-61` — float canonicalization is uneven: `h.pod(stroke)` on Rect/Triangle and the chrome hash use `ShapeStroke.width` raw f32 bits; Curve/Polyline canonicalize `width` through `canon_bits`. `corners` goes through `Corners: Hash` for shapes but `bytemuck::cast(corners)` raw for chrome. A `-0.0` or sub-`EPS` stroke splits the damage/measure key for some kinds and not others. `layout_core.rs` builds the hash tail with `to_ne_bytes` while the sibling `Gaps::resolved` shifts explicitly "so the key is the same number on either endianness".
- [ ] `src/primitives/brush/mod.rs:186-208`, `src/primitives/stroke.rs:52-58`, `src/primitives/shadow.rs:108-117`, `src/primitives/background.rs:36`, `src/primitives/color/mod.rs:58-73`, `src/scene/shapes/lower.rs:69` — the hand-written `Hash` for `Brush`, `Stroke`, `Shadow` and the derived one on `Background` have no consumer in palantir or darkroom; every production hash of paint goes through the lowered forms. Only `Gradient<G>: Hash` is read. Within `Brush::hash` the `Solid` arm calls `Color::hash` (eq-canonical) while the gradient arms hash geometry through `hash_visual`; `Stroke::hash` and `Shadow::hash` canonicalize colour visually. `Background`'s derived hash therefore mixes both policies, the split `FloatHash for Color`'s doc says a `Stroke` must not have.
- [ ] `src/scene/shapes/paint.rs:18-22` — `ShapeBrush` derives `Hash`, which would fold the frame-local `GradientId`; `hash_brush` exists precisely to avoid it and no production site uses the derive. A future `.hash()` on a record holding one silently hashes a per-frame index.
- [ ] `src/scene/node/configure.rs:124-137,139,143` vs `src/scene/node/theme_defaults.rs:47-62,74-93`; `src/widgets/theme/widget_look/look_plan.rs:68-76`; `src/widgets/text_edit/paint_input.rs:49,58`, `src/widgets/toggle_chrome/mod.rs:79-80`, `src/widgets/combo_box/mod.rs:116-118`, `src/widgets/context_menu/menu_item.rs:119-121`, `src/widgets/slider/mod.rs:139` — `Configure` and `ThemeDefaults` repeat the `debug_assert_valid_bounds` + `Some(value)` bodies for min/max; only `Configure` NaN-screens padding/margin through `checked_spacing`. `look_plan.rs` re-implements `default_padding` / `default_margin` as `get_or_insert`, and five widgets write `Node` fields directly. Three parallel ways to author a node, and only the first is NaN-screened.
- [ ] `src/scene/node/mod.rs:176-178,184,189,193-212`, `src/scene/node/node_mode.rs` — three ways to bind a layout-mode payload: `Node::grid()` → `NodeMode::PendingGrid` + `set_grid_def`; `Node::scroll(spec)` + `set_scroll_spec` / `scroll_spec` mutating in place; `Node::scrollbars(id)` resolved at construction. `NodeMode` exists only to hold `PendingGrid`, and `resolved()` panics on it — a two-phase construction only `Grid` / `Splitter` use, encoded as an enum every node carries.
- [ ] `src/layout/axis_ctx.rs:132-172`, `src/layout/pass.rs:84-95,290-301`, `src/layout/scroll/mod.rs:39-42,50-61`, `src/layout/canvas/mod.rs:56-58,87-96` — `resolve_node` spells X and Y as two parallel blocks: `outer_w` / `outer_h` and two seven-field `AxisCtx {..}` literals that differ only in `.w` / `.h`, `p_horiz` / `p_vert`, `m_horiz` / `m_vert`. The same shape recurs in `measure_per_axis_hug`, the `intrinsic_min` pair in `measure`, `Scroll::measure`, and `Canvas::measure` / `arrange`. `Axis` with `main` / `cross` / `compose_size` exists to make these one path.
- [ ] `src/layout/types/track/mod.rs:13-17,26-28,38-49` vs `src/layout/types/sizing.rs:112`, `src/layout/axis_ctx.rs:76` — `Track { size: Sizing, min, max }` is the same `(Sizing, min, max)` a node axis carries via `Sizes` + `BoundsExtras`, but resolves under `Track::content_floor` (`min_content.max(min).min(max)`) rather than `AxisCtx::resolve`'s `rendered.max(0).clamp(min, max)`. Constructor names cross over: `Track::fill_weight(w)` builds what `Sizing::fill(w)` builds while `Sizing::fill_weight()` is the accessor; `Track::fill()` ≡ `Sizing::FILL`, `Track::hug()` ≡ `Sizing::HUG`.
- [ ] `src/widgets/theme/button.rs:32-41,151-157`, `src/widgets/theme/text_edit.rs:50-57,184-190`, `src/widgets/theme/context_menu/menu_item.rs:146-160,231-237`, `src/widgets/theme/toggle.rs:220-227,369-375`, `src/widgets/theme/widget_look/theme_slot.rs:300-304` — four bundles each declare `padding: Spacing`, `margin: Spacing`, `anim: Option<AnimSpec>` with the same doc comments and the same serde attributes, then each hand-writes an identical `ThemeSlot::defaults` body rebuilding a `SlotDefaults` from those three fields. The type that names the triple exists; the bundles do not hold it.
- [ ] `src/widgets/popup/mod.rs:213-231,241-242` vs `src/widgets/modal/mod.rs:100-113`, `src/widgets/overlay_scope.rs:33-36,71-78,94,110`, `src/widgets/tooltip/mod.rs:217` — both overlays are "click-eater under a body, plus key scope, plus escape, plus withdraw", but only the key-scope half is shared through `OverlayScope`. `Popup` records its eater as a separate `Frame` in its own layer session and opens a second placed session for the body; `Modal` makes the root `ZStack` the eater with the card as its child in one session. `OverlayScope::passive` has one caller and reduces to `ui.layer().placement().show()` plus a constant `false`; `owns_keys` is a flag encoding which constructor ran. `Tooltip` does the placed-layer show directly without the type.
- [ ] `src/widgets/grid.rs:116-123`, `src/widgets/panel/mod.rs:38-42`, `src/widgets/popup/mod.rs:234-238`, `src/widgets/context_menu/mod.rs:137,147`, `src/widgets/combo_box/mod.rs:170`, `src/widgets/tooltip/mod.rs:207`, `src/widgets/modal/mod.rs:85` — the identical three-line `node.resolve_container_chrome(self.chrome, ui.theme().panel_background.as_ref(), ui.theme().panel_clip)` is written in three widgets, and each overlay resolves "explicit chrome or themed panel" its own way: `unwrap_or_else(|| ctx.panel.clone())` always passing `Some`, `ui_theme.context_menu.panel.clone()`, `self.chrome.as_ref().unwrap_or(&theme.panel)`, `unwrap_or(&mt.card)`.
- [ ] `src/widgets/combo_box/mod.rs:41-43,174-178` vs `src/widgets/context_menu/mod.rs:144-151` — the combo doc says the list "reuses the context-menu panel + `MenuItem` rows", but `ComboBox` applies only the panel background and a trigger-width `min_size`; `ContextMenu` also applies `default_padding(ctx.padding)`, `default_min_size(ctx.min_width)`, and `default_gap(ctx.gap)`. The two menus render differently from the same theme.
- [ ] `src/widgets/slider/mod.rs:20-35,87-89` vs `src/widgets/drag_value/mod.rs:151-165` — `SliderResponse` and `DragValueResponse` are field-for-field identical (`response`, `changed`, `committed`) with the same semantics; the slider doc acknowledges it mirrors the other. Two exported types for one shape.
- [ ] `src/widgets/drag_value/mod.rs:50-70,87-112` — `DragNum::commit_drag` and `DragNum::parse_from` each contain an `I64` arm and an `F64` arm, and all four do clamp → compare → write → return `changed`. The two `F64` arms repeat the `+ 0.0` sign normalisation and the `to_bits` comparison verbatim.
- [ ] `src/widgets/checkbox/mod.rs:40-49,59-63`, `src/widgets/switch.rs:38-47,58-61`, `src/widgets/radio/mod.rs:40-50` — `ToggleChrome::record_row` shares the row but not the entry: the three `new()` bodies are the same `Node::hstack()` + `Sense::CLICK` over the same four fields, and `Checkbox::show` and `Switch::show` open with the identical `if response.left.clicked() && !response.disabled { *self.value = !*self.value }`.
- [ ] `src/widgets/text_edit/unicode.rs:48,62,66-137,144-188`, `src/text/probe/mod.rs:22` — `CharKind` / `char_kind`, `next_word_boundary`, `prev_word_boundary`, and `word_range_at` implement a per-codepoint word classifier, while the same file already uses `unicode-segmentation`, which provides UAX #29 word boundaries (`split_word_bound_indices`). The comment at `:68-70` concedes it is worse on CJK "but isn't wired yet".
- [ ] `src/input/response/pointer_edge.rs:14-25` vs `src/input/response/button_phase.rs:29-59` + `src/input/response/drag.rs:25-44`, `src/input/input_state/mod.rs:309-338,847-894` — `PointerEdge::{Pressed{count}, Clicked{count}, DragStarted, DragStopped}` is the same four edges as `ButtonPhase::Down{press}`, `ButtonPhase::Up{click: Some(_)}`, `Drag::Started`, `Drag::Stopped`, and the run number travels under four names (`press`, `click`, `count`, `Press::seq` / `ReleaseKind::Click{count}`). Both vocabularies are derived from `Capture` by two independent `match` ladders.
- [ ] `src/input/input_state/mod.rs:431,438-453,551-588` — `ScrollPixels` and `ScrollLines` are the same nine lines with `pixels` / `lines` swapped; `Zoom` repeats the shape with `zoom::combine`. `PointerLeft` pushes `PointerEvent::Leave` directly and re-derives the "any pointer watch" gate as `!pointer_mask.is_empty()`, bypassing the `push_pointer_event` every other arm uses; `self.captures.iter().any(|c| c.press.is_some())` is spelled at both 431 and 442.
- [ ] `src/input/input_state/mod.rs:461,527` — the release arm re-derives "clickable under pointer" via the filter-taking `Cascade::hit_test(p, Sense::clicks)`, whose only other callers are the harness, while the press that created the capture used `hit_test_press`. Press and release answer one question through different entry points.
- [ ] `src/ui/state.rs:22-58,93-114` vs `src/animation/mod.rs:33-73` — `StateMap { stores: TypedStores }` and `AnimMap { stores: TypedStores }` are the same wrapper with different names and visibilities: `typed_mut` (private) vs `typed_mut` (`pub(crate)`), `try_get` / `try_get_mut` vs `try_typed_mut`, `sweep_removed` vs `sweep_removed` delegating to `sweep_removed_dropping_drained`. `Store<T>::sweep_removed` inherent plus a `TypedStore::sweep_removed` trait impl of the same name that only delegates to it.
- [ ] `src/input/shortcut/mod.rs:146`, `src/input/keyboard/modifiers.rs:50-52`, `src/input/key_class.rs:63-66,94` — three "is a command chord" predicates: `self.mods.ctrl || self.mods.alt`, `any_command` (`ctrl || alt || mac_ctrl`), and `is_edit_chord` retrying the physical key with no modifier gate of its own. `Mods` has no `any_command`.
- [ ] `src/renderer/render_owner_id.rs:9-12`, `src/renderer/texture_id_source.rs:19-27`, `src/primitives/text_epoch.rs:18` — three monotonic id mints, three shapes: `RenderOwnerId::reserve()` on a static `AtomicU64`, `TextureIdSource::reserve(&self)` on an `Rc<Cell<u64>>` instance, `TextEpoch` on another static atomic.
- [ ] `src/primitives/color/mod.rs:140-151` vs `:312,346` — `Color::rgb` / `rgba` read their arguments as sRGB and linearise; `ColorU8::rgb` / `rgba` store bytes as linear verbatim. The same names on two sibling colour types carry opposite colour-space contracts, while `hex` / `hexa` are consistently sRGB on both.
- [ ] `src/primitives/color/mod.rs:215-229,362-373,388-395` — `Color::hex`, `Color::hexa`, and `ColorU8::hexa` each hand-write the `>> 24 / >> 16 / >> 8 / & 0xff` byte split. `ColorU8::hexa` re-implements the three-channel `unit_to_u8` quantize that `From<Color> for ColorU8` already does.
- [ ] `src/primitives/color/mod.rs:26,188-195`, `src/primitives/brush/mod.rs:218`, `src/renderer/gradient_atlas/bake.rs:125`, `src/animation/anim_map_typed.rs:212`, `src/animation/animatable.rs:52-104` — `Color` has an inherent `const fn lerp` and a derived `Animatable::lerp` with identical per-channel math; two callers use one, two use the other. `impl Animatable for f32` and `for Vec2` are the same six one-line bodies written out twice.
- [ ] `src/text/cosmic/ellipsis_memo.rs:12-17`, `src/text/cosmic/mod.rs:374,388,751-760,854-860`, `src/text/key.rs:101-123,151-157,178-200` — `EllipsisMemo` restates `TextShapeKey`'s `size_q` / `family_q` / `weight_q` and compares them by hand in `advance_for`; `ellipsis_advance` takes four parameters that its caller derives one by one from the `key` it holds. `TextShapeKey::unbounded(hash, font)` folds a `GlyphFont` in, but there is no inverse: `shape_wrapped` and `shape_truncated` rebuild `Metrics` and `Attrs` from four separate accessor calls.
- [ ] `src/text/run.rs:75-77`, `src/text/shaper.rs:200-206` — `TextRun::unbounded_key` is a second key-minting path with one caller — the empty-text arm of `layout` — where it re-hashes text that `unbounded_request` hashed on the line above.
- [ ] `src/text/wrap/mod.rs:76`, `src/text/key.rs:277` — within each width-binding sequence the same width is canonicalized twice: once inside `resolves_to_unbounded` and again inside `WrapBound::new`.
- [ ] `src/icons/icon_rasterizer/mod.rs:158-177` — the mask path reads the raster through `rgba.as_chunks::<4>()` while the colour path reads the same bytes through `pixmap.as_ref().pixels()` and pushes four bytes per pixel with `extend_from_slice(&[..])`.
- [ ] `src/scene/forest.rs:298-302,313-318` vs `:327-352`; `src/scene/tree/mod.rs:133-136`, `src/scene/shapes/mod.rs:68` — `add_shape` routes through `push_shape` (open-node gate, disjoint borrows, `paint_rows += 1`); `add_shape_animated` re-spells the gate, the borrows, the `Shapes::add` call, and the bump inline. `push_shape` takes `what: &str` only to name itself in the debug assert. `ChromeInput { bg, store }` exists so `Tree::open_node` can call `lower::background(store, bg)`; the sibling `Shapes::add(shape, store)` takes the store as a plain second argument.
- [ ] `src/scene/tree/mod.rs:299,556,594-596`, `src/scene/cascade/paint_rect.rs:219`, `src/scene/tree/iter.rs:19-24` — `Tree::tree_items(node)` exists, yet three sites call `TreeItems::new(&tree.records, &tree.shapes.records, node)` directly (one is a split-borrow necessity, the other two are not). `ChildIter` is built by struct literal through `pub(crate)` fields from `Tree::children`, while `TreeItems` has private fields and a `new`.
- [ ] `src/scene/damage/walk.rs:156-159,241-244,289-290,364-366,380,384` — `LayerWalk::rows(i)` exists for "span → slice", yet `on_added`, `on_paints_changed`, and `on_subtree_moved` re-inline `self.cascade.paint_arena.node_spans[i]` + `rows[span.range()]` (five sites), including `span.len == 0` where `rows(j).is_empty()` is the helper's answer.
- [ ] `src/scene/cascade/paint_rect.rs:90-95`, `src/scene/damage/mod.rs:355-356,399-403`, `src/scene/damage/walk.rs:246-248,272-274,401-404` — "push a rect if it paints" is `push_paint` (cascade, also folds a union), `push_screen` (damage), an inline re-spelling at `walk.rs:402-404` in the file that imports `push_screen`, a `for` loop push at two sites, and an `extend` at a third.
- [ ] `src/scene/cascade/engine.rs:380-381`, `src/scene/damage/walk.rs:195-196,213` — "is this node a leaf?" is spelled three ways across the two walks: `ends[iu].end() != i + 1`, `subtree_end_of(i) == i + 1`, `end > i + 1`. The engine reads the raw column while the walk goes through `Tree::subtree_end_of`.
- [ ] `src/layout/wrapstack/mod.rs:103-127,202,213` vs `src/layout/stack/mod.rs:27-38,58,72-74,175,178,188,234-239,270,285` — `WrapScratch` hides its `Vec<NodeId>` behind four one-line forwarders (`len` / `push` / `at` / `truncate`); `StackScratch` hands the driver `.pool` directly and adds a `from(start)` slicer whose name reads as `From::from`. `WrapStack::arrange` stores `line_start` as `u32` only to cast it back to `usize`.
- [ ] `src/layout/counters.rs:11-19,44-50,73-77,86-93,138-171,223-228`, `src/layout/cache/mod.rs:223-224,436-441`, `src/common/counters.rs:32-36,251-292` — `PhaseTimings` and its three `add_*` mutators carry per-write `#[cfg(feature = "bench")]` blocks and `#[allow(unused_variables)]` parameters instead of a `BenchOnly<PhaseTimings>` cell; the module doc argues the closure body couldn't typecheck because `PhaseSpan::elapsed_ns` is gated, but `elapsed_ns` is itself the thing being gated. `ReplayCounts` plus `arrange_replays()` is a two-field snapshot assembled by hand next to `counter_snapshot!`. `MeasureCache::snapshot_rebuilds` is a raw `#[cfg(any(test, feature = "internals"))] u32` with a hand-gated `saturating_add`, gated on `internals` where the counters doc says bench-read counters gate on `bench`.
- [ ] `src/input/input_state/mod.rs:139-165`, `src/input/response/response_state.rs:74-90`, `src/primitives/translate_scale.rs:223-227` — both `Default` impls list every field at the field type's own default (`Modifiers::NONE == Modifiers::default()`, `InputSignal::None` is `#[default]`, `TranslateScale::default()` is `IDENTITY`, `[Capture; 3]`).
- [ ] `src/renderer/backend/raster_atlas/side.rs:76,133`, `src/renderer/backend/raster_atlas/mod.rs:92-99`, `src/renderer/backend/raster_atlas/bound_sides.rs:35` — `format!("{label} {} atlas", content.side_name())` is spelled in `Side::new` and again in `Side::grow`; `AtlasLabels.stem` is retained solely to feed the second, and `BoundSides` keeps its own `label: String` beside `AtlasLabels`'s three.
- [ ] `src/renderer/backend/gpu_timings.rs:464-466,478-480,500` — six hand-rolled `u64::from_le_bytes(ts[o..o+8].try_into().unwrap())` decodes over a mapped range in a crate that uses `bytemuck::cast_slice` everywhere else in the backend.
- [ ] `src/scene/damage/row_matcher.rs:128,138` — `diff_changed_leg` computes `prev_slice = &paints.slots[prev_start..prev_start + prev_len]` for the fast-path compare and then `prev = &paints.slots[prev_start..prev_start + prev_len]` again for the slow path — the same slice, bound twice.

## Logic lives on the wrong type, or a type's internals are edited from outside

- [ ] `src/renderer/backend/text/encode/cache.rs:62-87`, `src/renderer/backend/text/encode/encoder.rs:91-134,150,236` — `EncodedCache` owns `settle` and `sweep`, but the hit path — `map.get_mut`, indexing `arena.slots[entry.span.range()]`, `entry.last_use = frame`, `counters.hits.bump()`, the stale-row drop via `map.remove` + `arena.release` — is written inside `TextEncoder::try_emit_cached` by reaching through five `pub(crate)` fields. `encode_run` pushes straight into `cache.pending` and reads `cache.counters`. The type that documents its invariants does not own the code path that most often touches them.
- [ ] `src/renderer/backend/image_pipeline/textures.rs:13`, `src/renderer/backend/image_pipeline/render_target.rs:100-106,103,116-123,120,141,148`, `src/renderer/backend/image_pipeline/mod.rs:75-101,225`, `src/renderer/backend/mod.rs:1098` — `ImageTextures` exposes a `bind_group()` builder but no insert/remove, so `GpuViewTargets::ensure`, `paint`, and `retire_owner` all `textures.bindings.insert/remove` themselves, and `ImagePipeline::draw` probes the raw map. The "retain and drop the binding" idiom is written twice. Three one-line forwarders `ImagePipeline::drain_registry` / `paint_gpu_views` / `retire_render_owner` exist so `WgpuBackend` can reach `GpuViewTargets`; `retire_render_owner` is three levels of forwarding.
- [ ] `src/ui/mod.rs:142,853-875`, `src/ui/frame_cycle.rs:407` — `Ui::gpu_view` holds the upsert, the first-sight `texture_ids.reserve()`, and the epoch-bump-on-repaint policy inline over a bare `WidgetIdMap<GpuViewEntry>` field; the sweep for that same map is a second inline `retain` in `FrameCycle::finalize_frame`.
- [ ] `src/ui/mod.rs:539-554,658-671`, `src/window/window_requests.rs` — `open_window` and `drain_window_output` implement dedup-by-token over `commands.opens`, the veto→close settlement, the `append`, and the veto reset on `Ui`. `WindowRequests` has no methods; `WindowCommands` has only `append`.
- [ ] `src/ui/mod.rs:1201-1239`, `src/animation/mod.rs:33-73` — `Ui::animate` holds the snap / drop-stale-row / tick / settle policy, and line 1223 reaches into `AnimMapTyped::rows` with `typed.rows.remove(&(id, slot))` — `Ui` knows the typed map's key shape. `AnimMap` exposes only `typed_mut` / `try_typed_mut` / `is_empty` / `sweep_removed`.
- [ ] `src/widgets/text_edit/input.rs:147-181` vs `src/widgets/text_edit/editor.rs:15-23,248-259`, `src/widgets/text_edit/menu.rs:172-173` — the pointer path writes `ed.state.caret`, `ed.state.selection`, `ed.state.drag_anchor`, `ed.state.last_edit_kind` directly and maintains the "never `Some(caret)`" invariant by hand, rather than through `Editor::move_caret`, which exists to own that invariant. `Editor`'s `text` / `state` / `multiline` / `edited` are all `pub(super)`, which is what makes the bypass possible; `menu.rs` reaches in the same way.
- [ ] `src/widgets/text_edit/input.rs:44-53,67-75`, `src/widgets/text_edit/menu.rs:151-156` — `run_input` is a 7-parameter free function with `AcceptPolicy` existing, per its own doc, to shorten the list; `menu::show` is a free function over `&mut Editor`. Both are behaviour over `Editor` written outside it.
- [ ] `src/scene/record_store/mod.rs:34,38-40,47-52,73-75`, `src/scene/shapes/lower.rs:77,254,300` — `RecordStore.payloads` is a `RefCell` so `Lower::lower(self, &RecordStore)` and the `lower::*` functions can `borrow_mut()`. Every production writer reaches the store through `&mut Forest`; `RecordStore::clear` already takes `&mut self` and its comment concedes the cell is redundant there. Each shape lowering pays a borrow-flag check. `record_text` is a method that borrows immutably while the three staging functions reach into `store.payloads` directly.
- [ ] `src/layout/pass.rs:409`, `src/layout/cache/mod.rs:71,89-95,209` — `replay_arranged` indexes `self.engine.cache.previous.nodes.rect[base..]` directly, which is the only reason `MeasureCache::previous`, `NodeArenas`, and `NodeArenas::rect` are `pub(crate)` / `pub(super)`; every other snapshot read goes through `try_lookup` / `lookup_root_intrinsic`. `quantize_available` is `pub(crate)` with callers only in `pass.rs` and `engine.rs`.
- [ ] `src/renderer/frontend/paint_sink/mod.rs:86,123,181`, `src/renderer/frontend/encoder/mod.rs:103`, `src/renderer/frontend/encoder/layer_ctx.rs:96,381`, `src/renderer/frontend/composer/session.rs:178`, `src/renderer/frontend/capture.rs:87` — `PaintSink` has two implementors, and one (`PaintCapture`) is `#[cfg(any(test, feature = "bench"))]`. Production encodes through `&mut dyn PaintSink`, so every draw of every shape is a vtable call to a single concrete type, and `PaintGate` is a second trait over the same eleven methods whose only role is to attach the no-op gate to `dyn PaintSink`.
- [ ] `src/text/shaper.rs:36-59,131-156,252-260`, `src/text/system.rs:65,269-278` — in a production build `Metric` is a one-variant enum, so every measurement goes `TextShaper::root/resolve` → `ShaperInner::root/resolve` → `match Metric::Cosmic` → `CosmicMeasure::root/resolve`: three forwarding layers whose only reason is the gated mono arm. `TextSystem.shapes_buffers` is a cached copy of the same gated distinction and is always `true` in production.
- [ ] `src/renderer/backend/mod.rs:1102-1124` — `copy_backbuffer_into(&self, backbuffer, encoder, surface_tex)` reads nothing from `self`; the operation is "copy this backbuffer onto that texture", which `Backbuffer` owns every input for.
- [ ] `src/renderer/backend/mod.rs:359-374` vs `:389-401`, `src/host/window_driver/mod.rs:545-549`, `src/renderer/backend/backbuffer.rs:20-32,64` — `ensure_backbuffer(&self, &mut Option<Backbuffer>, size, format) -> bool` and `ensure_stencil(&self, &mut Option<Stencil>, size) -> &Stencil` solve the same lazily-recreate problem with different return contracts — one a flag the caller only `debug_assert!`s on, the other the borrowed value. Neither reads anything off `self` but `self.device`. `Backbuffer::new`'s doc is the paragraph that belongs on `describes`.
- [ ] `src/renderer/frontend/composer/geometry.rs:46-60,156-163`, `src/renderer/frontend/composer/session.rs:574` — `push_sub_instances(out: &mut RenderBuffer, ..)` pushes `CurveInstance`s into the buffer from the module described as "the arithmetic"; single caller. `stroke_bbox_urect` takes seven positional parameters including a whole `Display`, while its sibling `urect_from_phys` takes `viewport: UVec2`.
- [ ] `src/scene/seen_ids/mod.rs:47-50`, `src/layout/mod.rs:97,111`, `src/scene/cascade/entry.rs:79`, `src/ui/mod.rs:1647` — `Endpoint { layer, node }` is the crate's `(Layer, NodeId)` address, consumed by layout, cascade, and `Ui`; it is defined in a module whose concern is per-frame id collision bookkeeping.
- [ ] `src/primitives/arc/mod.rs:21`, `src/primitives/bezier/mod.rs:24,40`, `src/shape/stroke_bounds/mod.rs:17`, `src/primitives/num/mod.rs:40`, `src/common/hash/mod.rs:27`, `src/shape/curve.rs:15-37`, `src/scene/shapes/lower.rs:380-441`, `src/scene/record_store/text_store.rs:3` — `arc_bbox`, `cubic_bezier_bbox`, `quadratic_to_cubic`, `stroked_bbox`, `band_fraction`, and `hash_str` are `pub(crate)` free functions over the crate's own geometry; `CurveGeometry` is the value they are about and `lower.rs` destructures it just to hand the fields back. `hash_str` is imported bare in `text_store.rs` while every other caller keeps it `hash::hash_str`.
- [ ] `src/scene/cascade/engine.rs:21`, `src/scene/cascade/paint_rect.rs:12,13,17`, `src/scene/damage/walk.rs:24,33-37,422`, `src/scene/damage/row_matcher.rs:8`, `src/ui/frame_cycle.rs:369` — `compute_paint_rect`, `shadow_paint_rect_local`, `text_paint_bbox_local`, `stroked_bbox`, and `push_screen` are imported by name and called unqualified; the one caller outside these modules does it the other way (`cascade::engine::cascade_fingerprint(...)`). `emit_inverted_overlaps_into` is a free fn "so the pair enumeration can be measured and tested without a tree"; its only caller anywhere is `emit_inverted_overlaps`.
- [ ] `src/scene/shapes/paint.rs:231-243`, `src/scene/cascade/paint_rect.rs:193-201`, `src/renderer/frontend/encoder/layer_ctx.rs:560-567` — all three callers of `shadow_paint_rect_local` first call `shadow.geom()` and `shadow.inset()` and pass the four scalars plus two rects as six positional arguments; the function never takes the `LoweredShadow` it describes.
- [ ] `src/renderer/frontend/encoder/layer_ctx.rs:559-582`, `src/primitives/brush/gradient/mod.rs:171` — `emit_shadow` unpacks `LoweredShadow.geom_f16` through `geom()` and then re-packs the same four lanes with `FillAxis::from_lanes(offset.x, offset.y, blur, spread)` for the inset case. `FillAxis` is a `F16x4` newtype, so this is an f16→f32→f16 round-trip of identical bytes.
- [ ] `src/renderer/backend/text/encode/mod.rs:53-101`, `src/renderer/backend/text/encode/cache.rs:14-19`, `src/renderer/backend/text/mod.rs:137` — `EncodedEntry` (the map value of `EncodedCache`) and `encode_key_for` live in `encode/mod.rs`, while `EncodedGlyph` (the arena item of the same cache) lives in `cache.rs`. `encode_key_for` is a free `pub(super)` fn with one caller that takes a `&TextDrawRow` and a scale — a method-shaped signature.
- [ ] `src/ui/mod.rs:903-909`, `src/scene/record_store/mod.rs:58-66` — `Ui::intern` matches on `TextInput` while `RecordStore` offers only `intern_str` / `intern_fmt`; `Ui::fmt` and `Ui::intern` are `&mut self` over a store API that is `&self`.
- [ ] `src/renderer/backend/mod.rs:793-798,816-821` vs `src/renderer/backend/gpu_timings.rs:252-255` — `run_main_pass` gates `pass_begin` / `pass_end` on the `pub(super)` field `t.inside_passes`, while `mark` gates on the same field internally and `pass_begin` / `pass_end` `debug_assert!` it. The check is on the caller for two methods and inside the callee for the third.
- [ ] `src/scene/damage/region/mod.rs:76,135,141`, `src/scene/damage/mod.rs:224` — `DamageRegion::rects` is a `pub(crate)` field and the type also exposes `iter_rects` / `any_intersects`; `Damage::new` bypasses both with `damage.region.rects.is_empty()`. The field doc says "Mutate only through `add`", which the visibility does not enforce.
- [ ] `src/widgets/widget.rs:88-90`, `src/ui/mod.rs:1025`, `src/scene/forest.rs:218`, `src/scene/tree/mod.rs:389` — the doc states node and chrome both "travel by reference from here down … so neither … is re-copied per hop", but `Ui::open_node`, `Forest::open_node`, `Tree::open_node`, and `into_columns(self)` all take the 120-byte `Copy` `Node` by value — four hops relying on `#[inline]` to elide what the comment claims is structural.

## A sentinel or a flag encodes what the type already knows, or a builder is order-dependent

- [ ] `src/scene/node/gaps.rs:30-42,45-51,63-67`, `src/scene/node/mod.rs:254,262-266`, `src/scene/node/panel_extras.rs:21,46` — `Gaps::UNSET` (an f16 NaN lane) exists so `ThemeDefaults::default_gap` can tell "untouched" from an explicit `0.0`, an authoring-time question. `Node::into_columns` resolves every other themable `Option` but hands `self.gaps` through raw, so `Tree::panel_table` rows carry the sentinel; every layout read pays a `.max(0.0)` fold, and `PanelExtras::hash` / `is_default` route through `Gaps::resolved()` to keep an unset gap from splitting a cache key.
- [ ] `src/scene/node/mod.rs:50-74`, `src/scene/node/salt.rs:61,73-75` — "unset" is spelled three ways on one struct: `Option<T>` for six fields, the NaN lane for `gaps`, and `Salt::Auto` vs `Salt::Hash` / `Verbatim` for identity. `Salt::Auto` and `Salt::Hash` resolve identically; the variant split carries only the `is_explicit` bool.
- [ ] `src/layout/types/placement.rs:23-36`, `src/ui/layer_scope.rs:41,49` — `Placement::with_size` on an `Overlay` returns `Fixed { anchor: Vec2::ZERO, .. }`, so `.placement(overlay).max_size(..)` moves the layer to the surface origin; `with_anchor` does the mirror conversion. Whether an overlay survives depends on call order.
- [ ] `src/widgets/separator/mod.rs:19-21,88-95` — when no explicit size was set, `self.node.size(default_size).align(Stretch…)` overwrites `node.align` unconditionally, so a caller's `.align(...)` is silently discarded unless they also set `.size(...)`. The doc says only an explicit `size` replaces the default.
- [ ] `src/widgets/drag_value/mod.rs:254-259` — `editable(true)` replaces the node's sense with `CLICK | DRAG`, dropping any sense the caller set earlier, and `editable(false)` after `editable(true)` leaves `CLICK` set. The builder is order-dependent and not idempotent.
- [ ] `src/widgets/grid.rs:74-93,128`, `src/scene/node/configure.rs:199-203` — `Grid` implements `Configure` yet defines inherent `gap` / `gap_xy` on its own `row_gap` / `col_gap`; `Configure::gap` / `line_gap` invoked through the trait write `node.gaps`, which the grid ignores, with no error.
- [ ] `src/widgets/slider/mod.rs:52,58,71-76,188-193` vs `src/widgets/drag_value/mod.rs:206-248` — `Slider::new(&mut f32, RangeInclusive<f32>)` takes the range in the constructor and binds only `f32`; `DragValue::new(impl Into<DragNum>)` binds `i64` / `f64` and takes `.range(RangeInclusive<f64>)` as a builder. Quantisation is `.step(f32)` anchored at `min` on one and `.decimals(usize)` plus `.speed` on the other. `Slider.step` is `Option<f32>` but `snap_to_step` treats `None` and `Some(s <= 0.0)` identically.
- [ ] `src/widgets/slider/mod.rs:166` vs `src/widgets/splitter/mod.rs:239,260`, `src/primitives/num/mod.rs:40` — "no span" is answered with two predicates: `approx::approx_zero(span)` (NaN-false) in `Slider::value_to_fraction` and `approx::noop_f32` (NaN-true) in `Splitter`; `num::band_fraction` goes through `approx::ratio`, also `noop_f32`. The NaN-unsafe choice is on the path that ends in the release assert.
- [ ] `src/text/cosmic/mod.rs:308-311,418-421,532-534`, `src/text/system.rs:213,304-309` — `supersede` silently returns on `TextShapeKey::INVALID` while `shaped_run` and `ensure_buffer` `debug_assert` against it. Neither caller of `supersede` can pass it — both derive from a `TextShapeRequest` key, which `content_hash` keeps nonzero.
- [ ] `src/renderer/backend/text/mod.rs:132-136`, `src/renderer/backend/text/encode/encoder.rs:202-205`, `src/renderer/backend/text/encode/cache.rs:399` — `if r.text.key.is_invalid() { continue; }` with a comment "nothing production-emitted reaches this"; the crate's convention elsewhere is a `debug_assert!` for such contracts.
- [ ] `src/text/glyphs/mod.rs:146-152`, `src/text/cosmic/mod.rs:639-650` — `TextGlyphs::placement` encodes "no bounds" as `URect::new(0, 0, u32::MAX, u32::MAX)` and `extract_glyphs` runs both y-cull comparisons against it every call, instead of the bounds being optional.
- [ ] `src/common/clipboard.rs:152-158`, `src/widgets/text_edit/action.rs:120` — `Clipboard::get` collapses `ClipboardUnavailable` to `""` via `unwrap_or_default` while `set` returns the `Result`; a paste from an unavailable clipboard is indistinguishable from an empty one.
- [ ] `src/widgets/scroll/state.rs:338,366-369` — `drag_anchor: Option<(Axis, f32)>` is an anonymous pair where the sibling `ThumbTravel` / `TrackPage` were named for exactly the "reads as nothing when destructured" reason.
- [ ] `src/widgets/text_edit/mod.rs:397-409`, `src/widgets/theme/text_style.rs:172`, `src/shape/text.rs:83` — `!look.text.metrics_valid()` early-returns and paints an empty box. `TextStyle` can only be invalid when built by hand (`try_from` and `Theme::scale_text` both validate), so this is a logic error being swallowed; no other themed widget has the branch, so `TextEdit` alone carries a second `EditSignals` construction for it.
- [ ] `src/widgets/text_edit/input.rs:262` — `Key::Escape if !editor.collapse_selection()` performs the collapse inside a match guard; the side effect is in the pattern.
- [ ] `src/scene/node/configure.rs:183,193`, `src/lib.rs:255` — `grid_cell((row, col))` / `grid_span((rs, cs))` take bare `(u16, u16)` tuples while `GridCell` is a `pub` re-export with those four fields; there is no setter taking a `GridCell`.
- [ ] `src/primitives/lut_row.rs:19,27` — `LutRow` spells row 0 twice: the derived `Default` and `FALLBACK`.
- [ ] `src/primitives/serde/mod.rs:85-117`, `src/primitives/size.rs:136-149` — `LaneVisitor::visit_map` silently zero-fills absent lanes (`{tl: 4}` → `[4, 0, 0, 0]`), while the array form rejects a 3- or 5-node array and `Size`'s table reads an absent axis as infinity. Sibling wire formats disagree on what an omitted lane means.
- [ ] `src/window/window_directory.rs:19-31` — `set_live(token, live: bool)` is one method whose `bool` selects between insert-with-assert and remove-with-expect; both callers pass a literal.
- [ ] `src/renderer/frontend/payload/draw_image_payload.rs:66`, `src/renderer/frontend/paint_sink/mod.rs:106,200-205` — `DrawImagePayload::is_noop(&self, is_gpu_view: bool)` is the one `is_noop` that takes an argument; the `paint: Option<&GpuPaintRef>` fact rides beside the payload through `PaintSink::image` rather than inside it, so `image` is the one sink method with two parameters and the one gate written outside the `noop_gates!` table.
- [ ] `src/scene/cascade/engine.rs:555-561`, `src/scene/damage/walk.rs:211-219`, `src/scene/damage/mod.rs:18-27`, `src/scene/damage/node_snapshot.rs:9-12,44-46` — `compute_node_paint` writes an empty span for any node whose own `Visibility` is not `Visible`, regardless of children. `LayerWalk::classify` routes a no-snapshot node to `Tier::Untracked` only when `childless`; a `Hidden` node with children is `Tier::Added`, and `on_added` stores `paints.store(&[])` and inserts a `NodeSnapshot`. That contradicts the row invariant stated in three places ("`prev` only holds entries for widgets with at least one paint row", "`paint_span` … Never empty"). The effect today is an unneeded map entry per hidden container.
- [ ] `src/layout/pass.rs:234` vs `src/layout/engine.rs:318`, `src/scene/tree/mod.rs:314-317` — leaf text is shaped for `Visibility::Hidden` nodes (measure returns early only on `is_collapsed`), but container text is skipped for them (`!is_visible()`), even though `compute_rollups` sets the `container_text` bit regardless of visibility. Hidden leaves pay shaping for paint that never happens; hidden containers do not.

## The demo surfaces re-roll the shared workload and scaffolds by hand

- [ ] `src/frame_fixture/specimen.rs:207-223`, `src/shape/mod.rs:324`, `src/primitives/mesh/mod.rs:71,76`, `src/bin/showcase/pages/shapes.rs:168-193` — `gradient_mesh` launders a leaked `Box<Mesh>` through `OnceLock<usize>` and `unsafe { &*(ptr as *const Mesh) }` to get a `&'static Mesh`. The justification comment ("`Shape::mesh` borrows a `&'static Mesh`") is false: `Shape::mesh` is `pub fn mesh(mesh: &Mesh) -> MeshShape<'_>`. The `usize` cast exists only because `Mesh` is `!Sync`. The showcase solves the identical problem with a plain `ui.with_state::<Mesh, _>` row.
- [ ] `src/bin/showcase/pages/pan_zoom.rs:111-127,129-149,151-205,207-229,274-316` vs `src/frame_fixture/forms.rs:171-235`, `src/frame_fixture/lists.rs:42-54,68-135`, `src/frame_fixture/chrome.rs:34-91` — the "heavy document" is a hand-copied subset of `FrameFixture`, described in its own doc as "for benchmarking": `property_grid` vs `forms::properties_card` (same tracks, salts, "Edit" button, first eight `LABELS` / four `VALUES`), `chat_messages` vs `lists::activity_card` (same salts, `user_{i}`, 40/34 px avatar, Lorem body), `header_band` vs `chrome::app_bar`, `gradient_strip` vs the filmstrip thumb. Two workloads drift independently, and the fixture's node-count freeze protects only one. `src/bin/showcase/pages/text.rs:169-187` `chat_row` is a third avatar-plus-Fill row.
- [ ] `src/bin/showcase/pages/icons.rs:82-98`, `src/bin/showcase/pages/images.rs:120-122`, `src/ui/mod.rs:1128,1164` — these two pages park their loaded `IconSet` / four `ImageHandle`s in `thread_local!` statics, while every other page keeps cross-frame state in a `ui.with_state` row keyed by `WidgetId::from_hash("showcase::…")`. `with_state` requires `S: Default`, but `Option<T>: Default` for any `T`. The statics outlive the `Ui` that minted the handles: a second `Ui` on the same thread gets an `IconSet` whose `Rc<IconSetToken>` holds a different registry.
- [ ] `src/bin/showcase/pages/shapes.rs:107-151,162-193` — `mesh_triangle`, `polygon_star` (a `Vec::with_capacity(10)` plus a `Mesh::new()`), and `gradient_quad` each build a fresh `Mesh` every frame, so the shapes page allocates three-to-four times per frame at steady state and re-hashes each mesh on every lower. The `stress` cell on the same page retains its mesh precisely because "a fresh `Mesh` per frame would allocate … and re-hash".
- [ ] `src/bin/showcase/pages/sizing.rs:32-38,274-289`, `src/bin/showcase/pages/text.rs:29-35`, `src/bin/showcase/pages/controls.rs:60-64,73-77`, `src/bin/showcase/pages/containers.rs:196-211,233-239`, `src/bin/showcase/pages/clip.rs:136-142`, `src/bin/showcase/support.rs` — `column(ui, id, body)` is defined twice, byte-identical; the two-column root is hand-rolled a third time in `controls.rs`. Six local helpers draw the same "fixed-size `Frame` with `swatch_bg(c)`" leaf (`chip`, `aligned_chip`, `sw`, `badge`, `positioned`, `tile`), and `sizing::chip` and `containers::chip` share a name for different things. `support.rs` owns `section` / `row` / `tiles` but no two-column scaffold.
- [ ] `src/bin/showcase/pages/dialogs.rs:101-127,150-194`; `src/bin/showcase/shell.rs:563-569` vs `src/bin/showcase/pages/state.rs:56-74`; `src/bin/showcase/pages/shadows.rs:75-79/159-163,85-89/167-171,126/176` — the confirm modal and the exit modal are the same tree written twice. The inspector open/close toggle is implemented once for F8 and once for the page button. The soft, elevated, and inset `Shadow::drop(...)` parameters are written once for the shape route and again for the chrome route; nothing shares the values.
- [ ] `src/bin/showcase/pages/motion.rs:141,148-152` vs `src/bin/showcase/pages/text_edit.rs:150-151`, `src/bin/showcase/pages/shapes.rs:168-169`, `src/bin/showcase/pages/dialogs.rs:33-39` — first-frame seeding is done with an `inited: bool` flag in one page and with `ui.try_state::<T>(id).is_none()` followed by `ui.with_state::<T, _>(id, …)` (two lookups) in two others. `dialogs.rs` wraps its ids in `state_id()` / `exit_state_id()` while every other page computes them inline.
- [ ] `src/demo_swatches.rs:24-32`, `src/bin/showcase/support.rs:52-53,60-68`, `src/frame_fixture/tokens.rs:35-38`, `src/frame_fixture/specimen.rs:153-160`, `src/bin/showcase/shell.rs:501-513` — five colours exist so two surfaces can share them, and each surface renames them (`TEAL` → `A` / `ACCENT`, …), so every swatch has three names; `specimen.rs` reaches past `tokens` to `demo_swatches` directly. `support::ACCENT` is `0x4cd3ff` but the installed theme sets `Palette.accent = 0x7fd6f5` and `text_disabled = 0x5f6673` (vs `INK_FAINT 0x6d7482`), so checked toggles use one teal and the nav rail another. `elem` / `elem_hover` / `elem_active` are a second surface ladder beside `support`'s `RAISED` / `BORDER` / `HAIRLINE`.
- [ ] `src/bin/showcase/pages/pan_zoom.rs:360-392`, `src/bin/showcase/pages/text_edit.rs:153,167`, `src/bin/showcase/pages/icons.rs:81-112,202-208` — `cell_theme` constructs a full `ButtonTheme` (four `WidgetLook`s + `Option<TextStyle>`) per cell per frame — 576 cells on the grid page — where only the fill colour varies; the label `TextStyle` is cloned three times per cell. `align_grid` allocates `format!("{vname} / {hname}")` for every empty cell each frame. `icons(ui)` is called at the top of every frame and each time clones the `IconSet` and does four `by_name` lookups; the ids are constant for the life of the set.
- [ ] `src/frame_fixture/chrome.rs:72,117,123,142,178`, `src/frame_fixture/lists.rs:55,105,114,152,169`, `src/lib.rs:196-201` — the fixture spells `ui.fmt(format_args!(...))` ten times; `fmt!` is exactly that and is what the showcase uses. No production code in the crate uses its own exported macro.
- [ ] `src/bin/showcase/pages/{clip,shapes,strokes,gradients,motion,pan_zoom,shadows,images,fixtures}.rs`, `src/bin/showcase/pages/overlays.rs:71`, `src/lib.rs:294-297` — every page does `use glam::Vec2` (and one writes `glam::Vec2::new` inline) although `lib.rs` re-exports `palantir::Vec2` exactly so consumers need no glam import.
- [ ] `src/lib.rs:95-99,136-141,210-214`, `tests/alloc/gates.rs:21` — the `internals` doc says it "is the only door out of the crate for integration tests", yet `FrameFixture` and `demo_swatches` are gated re-exports at the crate root, and the alloc gates import `palantir::FrameFixture` from the root while taking `BENCH_*` from `palantir::internals`. Two gated doors for one fixture.
- [ ] `src/hot_struct_sizes.rs:173-183,203-210,220`, `src/renderer/frontend/payload/draw_icon_payload.rs:14`, `src/renderer/render_buffer/{icon,image,mesh}.rs`, `src/layout/cache/mod.rs:126` — six encoder↔composer payloads are pinned but `DrawIconPayload` is not; `TextDrawRow` is pinned but `IconDrawRow` / `ImageDrawRow` / `MeshDrawRow` are not; `MeasureSnapshot` is absent though `NodeSnapshot` is pinned. Eleven value-bound widget builders are pinned but `Panel`, `Frame`, `Grid`, `Scroll`, `Separator`, `Spinner`, `Popup`, `Modal`, `Tooltip`, `GpuView`, `ContextMenu`, `MenuItem` are not, so a field added to `Panel` passes silently.
- [ ] `src/bin/showcase/shell.rs:229`, `src/bin/showcase/pages/frame_bench.rs:1-2`, `src/bench/driver.rs:71-77`, `src/bench/cli/mod.rs:173-179`, `Cargo.toml:105-108` — the frame-bench blurb says the workload is what `cargo bench --bench criterion -- '^frame/'` records; `frame` is the only `opt_in` driver and `Cli::selects` excludes opt-in drivers whenever `--driver` is absent, so that command runs zero frame benchmarks. `frame_bench.rs` names a `frame` bench target that does not exist.
- [ ] `src/bench/cli/mod.rs:91-97,139-141`, `src/bench/mod.rs:199` — `Cli::parse_args()` is `Cli::parse()` with one caller. `--sample-size`, `--measurement-time`, `--warm-up-time` carry no doc comment, so `--help` prints them without help text while every neighbouring flag has one.
- [ ] `src/golden/mod.rs:44-50,159-165` — `assert_matches` panics on a dimension mismatch, then calls `Tolerance::diff`, which `assert_eq!`s the same dimensions with a second message. Only one can fire.

## Dead code, unreachable branches, and machinery with no consumer

- [ ] `src/renderer/backend/raster_atlas/mod.rs:523-545`, `src/renderer/backend/raster_atlas/packed_metadata.rs:16-21`, `src/renderer/backend/text/encode/encoder.rs:221,297-315`, `src/renderer/backend/raster_atlas/free_slots.rs` — an unallocated slot's `left`, `top`, and `content` are never consumed: the encoder `continue`s on `alloc.is_none()` before reading bearing, the icon backend skips on zero size, `ClockSweep` rejects on `is_packed()` regardless of `content`, and `FreeSlots::release` reads `content` only inside `if let Some(id) = slot.alloc.take()`. The `content` and `metadata` parameters of `insert_unallocated`, the `debug_assert!(metadata.is_empty())`, and `PackedMetadata::EMPTY` exist to fill fields nobody reads.
- [ ] `src/renderer/backend/icon/mod.rs:199,232-241`, `src/icons/icon_raster_key/mod.rs:60-66` — `IconRasterKey::for_box` guarantees both axes ≥ 1 and `key.size` is `U16Vec2`, so `PackedMetadata::new(..)?` cannot fail, `metadata.is_empty()` is never true, `insert_unallocated` is never reached from the icon tenant, and the `slot.width == 0 || slot.height == 0` skip never fires.
- [ ] `src/renderer/render_buffer/text.rs:19-26` — `TextDrawRow` derives `bytemuck::Pod` / `Zeroable` with `#[repr(C)]` and a "Layout" paragraph justifying field order, but no `cast_slice` / `bytes_of` reads it anywhere; it is never uploaded. `ImageDrawRow` / `MeshDrawRow` carry the Pod on the inner `*Instance` that is uploaded.
- [ ] `src/renderer/gradient_atlas/shared_gradient_atlas.rs:52-55`, `src/renderer/backend/gpu_gradient_atlas.rs:140-162` — `flush_with<R>(.., impl FnOnce(FlushedRows) -> R) -> Option<R>` has one caller, with `R = ()`, and the `Option<()>` is dropped.
- [ ] `src/scene/shapes/record/mod.rs:211`, `src/scene/cascade/paint_rect.rs:257-302` — the `Polyline | Curve => *bbox` arm of `ShapeRecord::bbox_local` is unreachable in production: the only caller handles those two variants itself and lists only `Quad | Mesh | Image | Icon`. With the `Text` arm that `unreachable!`s, three of seven arms exist for a dispatch the caller has already done.
- [ ] `src/primitives/rect/mod.rs:109,250-257,286-292`, `src/primitives/urect/mod.rs:124`, `src/renderer/backend/schedule/mod.rs:270,440,539`, `src/ui/harness/mod.rs:640` — `Rect::intersect` has zero callers; all three `.intersect(` sites are on `URect`. It is also the one emptiness test in the file spelled `> 0.0`, where `Rect::union` and `URect::intersect` use `is_paint_empty`, so it would report `Some` for a sub-`EPS` sliver every other gate treats as empty. `Rect::center` has no production caller.
- [ ] `src/primitives/translate_scale.rs:101,129,147`; `src/primitives/corners/mod.rs:40-78,101-111`; `src/primitives/span.rs:33-35,38-70`, `src/scene/tree/mod.rs:362-363`, `src/primitives/mesh/mod.rs:272`; `src/primitives/brush/mod.rs:115-120`; `src/primitives/mesh/mod.rs:186,222-225`; `src/animation/anim_spec/mod.rs:66-71`; `anim-derive/src/lib.rs:19-20,184`; `src/primitives/color/mod.rs:288-313` — public API with no production caller: `TranslateScale::from_scale_about` / `from_translate_scale_about` (`pivoted_translation` was factored for three constructors, of which only `anchored_at` is reached); `Corners::top` / `bottom` / `left` / `right` / `top_bottom` / `diag_main` / `diag_anti` and the `From<Vec2>` / `From<Size>` impls; three of the four `Span` `From` impls (the used one truncates with `as u32` where `Mesh` guards the same conversion with `checked_vertex_index`); `Brush::as_linear` (one darkroom test); `Mesh::with_known_bbox` (none) and `Mesh::append` (documented as none); `AnimSpec::MEDIUM`; the `#[animate(skip)]` alias; `ColorU8::rgb` / `WHITE` / `BLACK` (a visual fixture and an inline test only).
- [ ] `src/layout/types/track/mod.rs:95-101`, `src/layout/types/sizing.rs:126-133,145-155`, `src/scene/tree/mod.rs:319` — `impl Hash for Track` has no production caller (the tree hashes tracks through `GridDef::hash_visual`); `impl Hash for Sizing` is reached only through it. `Sizing::hash_visual` and `Hash` differ only by `canon_bits` vs `eq_bits`.
- [ ] `src/layout/grid/grid_track_store.rs:78-91`, `src/layout/grid/measuring.rs:31`, `src/layout/driver.rs:75-77`, `src/layout/layout_scratch.rs:144` — `reset_hugs` zeroes four slices per grid per measure, documented as guarding "`LayoutEngine::measure`'s grow-driven second pass". `LayoutDriver::measure` is now "called exactly once per measure" and `GridTrackStore::reset_for` zeroes every pool at the top of each layer, so the fills are redundant.
- [ ] `src/layout/intrinsic/mod.rs:256,279-297` — `content_intrinsic` has one caller (`compute`) and only constructs an `IntrinsicOp` and calls `dispatch`.
- [ ] `src/widgets/popup/mod.rs:48` — `ClickOutside::Block` has no caller outside `popup/tests/`; nothing in the crate, the showcase, or the fixtures records a `Block` popup.
- [ ] `src/widgets/theme/button.rs:139-149`, `src/widgets/theme/text_edit.rs:85-87,180-182`, `src/widgets/theme/context_menu/menu_item.rs:181-183,227-229`, `src/widgets/theme/toggle.rs:260-266,365-367`, `src/widgets/theme/context_menu/mod.rs:73`, `src/widgets/theme/combo_box.rs:27` — each bundle has a `pub fn pick` whose only caller in the crate is its own `ThemeSlot::look`; two names and a public surface for one state pick per bundle. `ContextMenuTheme::with_radius` has no caller outside its tests. `ComboBoxTheme::from_palette(_p: &Palette)` ignores its argument; it exists only so `palette_default!` fits.
- [ ] `src/widgets/text_edit/edit_state.rs:14,89,229`, `src/widgets/text_edit/mod.rs:52`, `src/widgets/text_edit/view_state.rs:17`, `src/widgets/theme/text_style.rs:15`, `src/ui/mod.rs:1128` — `UNDO_LIMIT` (`pub(super)`), `repair_offset` (`pub(super)`), and `LINE_HEIGHT_MULT` (`pub(crate)`) have no production caller outside their own file. `TextEditState`, `EditState`, `ViewState` derive `Clone` over a `VecDeque<EditDelta>`, a `Vec<EditDelta>`, and a `Vec<Rect>`; `Ui::state_mut` requires only `Default + 'static` and nothing clones them.
- [ ] `src/renderer/frontend/payload/draw_curve_payload.rs:21`, `src/renderer/frontend/payload/draw_polyline_payload.rs:20`, `src/renderer/frontend/payload/stroke_bounds.rs:38-42`, `src/renderer/gpu_paint/gpu_paint_ref.rs:21-28` — `DrawCurvePayload` and `DrawPolylinePayload` derive `Default` and `StrokeBounds` hand-writes one; no production constructor uses any of them. `PartialEq for GpuPaintRef` exists only so the test-gated `PaintCall` can derive `PartialEq`; the one comparison helper deliberately compares `Debug` output instead.
- [ ] `src/renderer/frontend/encoder/collision_overlay.rs:50-58`, `src/renderer/frontend/encoder/mod.rs:9-10` — the module is `#[cfg(debug_assertions)]`, so the "release safety net" `if ep.node.idx() >= rects.len() { continue; }` after the `debug_assert!` can never execute in a build where the assert is absent.
- [ ] `src/host/window_driver/mod.rs:379-382`, `src/host/core.rs:87-103`, `src/host/winit/runtime.rs:97,112-122,231-238`, `src/host/winit/mod.rs:309-317`, `src/input/input_state/mod.rs:212-214,274-276,648-650`, `src/scene/cascade/mod.rs:274-278`, `src/renderer/frontend/composer/session.rs:237-239,1167-1178` — one-caller wrappers: `finish_cpu_frame` has no production caller of its own; `HostCore::cpu_frame` / `submit` forward one call each; `slot_of_id` / `window` / `by_token` forward to `WindowSet`; `register_window` wraps the same push `new` writes inline; `finish_run` is a four-line match; `finish_record` wraps `take_action_flag`, whose only caller is `finish_record`; `silenced(reader)` wraps `self.scopes.silences(reader)` verbatim; `hit_first` adds a `gate` closure that `hit_test` immediately fills with its own; `enter_clip` / `leave_clip` wrap the trait's `clip` / `pop_clip`.
- [ ] `src/scene/forest.rs:290`, `src/ui/mod.rs:410`, `src/display.rs:101-108` — `Forest::is_recording` has a single caller inside a `debug_assert!`, so it is dead in release. `Display::from_physical` is `pub` but reached only from the harness, benches, and a test module; both real hosts mint `Display` through `WindowDriver::display`.
- [ ] `src/lib.rs:164,259,261-270,298,300,330` — exports with no consumer outside the crate: `ConicGeometry` / `LinearGeometry` / `RadialGeometry` (no references outside their own file; reachable only through the `*Gradient` aliases beside them), `BatchKind` / `GpuPassStats` / `PipelineStats` (doc: "Consumers (debug overlay, benches)" — both in-crate), `IconDef`, `Span` (a `#[repr(C)]` index range in no public fn signature), `GlyphImageKind`, `GlyphPlacement`, `MeshVertex` (only a `pub(crate)` field of `Mesh`), the `Animatable` derive, and `AnimSlot`.
- [ ] `src/icons/icon_atlas.rs:24,105,114-124`, `src/icons/svg_facts.rs:15-24`, `src/icons/icon_set.rs:116-117` — `IconDef::name` is `&'static str` so the baked `const` table can hold it, which forces `IconAtlas::from_svgs` to take `(&'static str, &'a str)` pairs — a set built at runtime from data cannot supply names without leaking them. `SvgFacts` holds `view_box` / `tintable` / `filtered` and `from_svgs` copies them into `IconDef` field by field.
- [ ] `src/text/cosmic/mod.rs:23-26`, `src/text/shaped_ref.rs:21-24` — shaped buffers are keyed on a 64-bit `hash_str` of the text and never verified against the bytes on a hit, so a collision silently draws another run's glyphs. The accepted risk is documented, but `ShapedTextRef` already carries the exact source span the check would need.

## Visibility is wider than any caller, or differs between siblings

- [ ] `src/renderer/backend/mod.rs:96,104,112,115,117,572`, `src/renderer/backend/pipeline_recipe.rs:18-27`, `src/renderer/backend/stencil_variant.rs:27-33` — `gpu_ctx`, `pipeline_recipe`, `stencil_variant`, `texture_binding`, `viewport` are `pub(crate)` with no user outside `src/renderer/backend/`. `GpuCtx` and `GpuCtx::new` are likewise. `PipelineRecipe` and `ColorVariantSpec` fields are bare `pub` inside `pub(super)` structs.
- [ ] `src/renderer/backend/raster_atlas/mod.rs:143,159,179,198,287`, `src/renderer/backend/text/encode/encoder.rs:150` — `slots`, `cache`, `current_frame`, and `counters` are `pub(crate)`; the encoder reads `atlas.cache.len()` only for the starvation warning and `atlas.current_frame` in three places to stamp its own entries. `draw_span` is `pub(super)` while every sibling method on the same type is `pub(crate)`, though all callers sit under `backend`.
- [ ] `src/renderer/backend/stencil.rs:30` vs `src/renderer/backend/backbuffer.rs:55-62`, `src/host/window_driver/mod.rs:496`, `src/renderer/backend/submission.rs:17-21` — `Stencil.view` is a `pub(crate)` field reached as `.view` from the window driver, while `Backbuffer` keeps `view` / `texture` private behind accessors. `SubmissionTargets` carries `backbuffer: Option<&Backbuffer>` but `stencil: Option<&wgpu::TextureView>`.
- [ ] `src/renderer/frontend/composer/text_grid/mod.rs:54,70,100,122,129,157,169,221`, `src/renderer/frontend/composer/mod.rs:23-25` — `TILE_SIZE`, `TILE_CAP`, `spill`, and every `TextRectGrid` method are `pub(crate)`, yet every reader is inside `composer/`; `union` on the same struct is `pub(super)`. `pub(crate) mod text_grid` carries a comment explaining it exists only for the bench facade.
- [ ] `src/scene/cascade/engine.rs:189`, `src/scene/cascade/mod.rs:156,194` — `CascadeEngine::run_full` is `pub(crate)`; every production caller is inside `engine.rs`, and the bench reach-in is a sibling `pub(super)` covers. `Cascade::hits` is `pub(crate)`; outside tests it is read only by `hits_under` in `mod.rs` and written by `engine.rs`. `LayerCascade::entries_base` is `pub(super)` while `static_hash`, `paint_cardinality`, `layout_hash`, `subtree_hashes`, `subtree_ends` on the same struct are private and reached the same way.
- [ ] `src/scene/tree/mod.rs:385,528`, `src/scene/tree/mod.rs:145,152` — `Tree::close_node` is `pub(crate)` while `open_node` is `pub(super)`; both have one production caller in `Forest`. `subtree_end_of(i: usize)` / `subtree_has_grid(i: usize)` take a raw index while `children`, `bounds`, `panel`, `chrome`, `transform_of` on the same type take `NodeId`.
- [ ] `src/layout/pass.rs:68`, `src/layout/grid/grid_track_store.rs:72,100-101,107`, `src/layout/grid/grid_context.rs:16-18`, `src/layout/mod.rs:13-15,24` — `measure_per_axis_hug` is `pub(crate)` while every sibling accessor on `LayoutPass` is `pub(super)`. `GridTrackSlot` is `pub(crate)` with private fields and no use outside its file; `GridTrackStore::alloc` is `pub(super)` with both callers in the same `impl`; `GridContext`'s three fields are `pub(crate)` and read only inside `layout`. `axis_ctx`, `axis_align_pair`, `axis_placement`, `justify_offsets` are `pub(crate) mod` while every item inside is `pub(super)`.
- [ ] `src/widgets/widget.rs:73` vs `src/widgets/response.rs:47-48,68` — `Widget::response` is `pub(crate)` while `Widget`, `Widget::record`, `Widget::show`, and `Response::eager` are all `pub`; the `Response` doc says "External widget authors reach this through `Widget::response`". The eager path's opening half is closed to external authors while its closing half is public.
- [ ] `src/input/input_state/mod.rs:131`, `src/ui/state.rs:28`, `src/ui/mod.rs:1129` — `frame_pointer_events` is `pub(crate)` beside the `pointer_events(reader)` accessor; no production reader outside the file exists. `StateMap::get_or_insert_with` is `pub(crate)` while `try_get` / `try_get_mut` / `sweep_removed` are `pub(super)`; its one caller is `Ui::state_mut`.
- [ ] `src/ui/frame_stamp.rs:14-23` vs `:33-37`, `src/host/window_driver/mod.rs:373` — `FrameStamp` hides `pub(super)` fields behind `new`, while its wrapper `FrameInput` exposes `pub(crate)` fields and is built by literal in the driver — two siblings in one file with opposite construction shapes.
- [ ] `src/text/system.rs:61` — `TextSystem.shaper` is `pub(super)` but no production code outside `system.rs` reads it; the widening exists for `src/text/tests/*`, which the crate's own convention routes through gated `test_support` modules instead.
- [ ] `src/scene/seen_ids/mod.rs:83,103,123`, `src/primitives/widget_id/mod.rs:11-13,56`, `src/text/system.rs:141`, `src/ui/state.rs:51`, `src/animation/mod.rs:72` — `counters: FxHashMap<WidgetId, u32>`, `removed: FxHashSet<WidgetId>`, `discarded: FxHashSet<WidgetId>` re-hash `WidgetId` through `FxHasher`, while `curr` / `prev` use `WidgetIdMap`, which exists to skip exactly that. `removed` is probed once per retained entry by every `sweep_removed` consumer.
- [ ] `src/renderer/texture_limit.rs:4,61`, `src/renderer/image_registry.rs:58,80`, `src/display.rs:122`, `src/ui/mod.rs:21,69,71-72,96-99,385,1397,1410,1439,1446`, `src/host/winit/input/mod.rs:21` — inline `glam::UVec2` / `glam::Vec2` paths beside or without an import; `ui/mod.rs` imports `InternedStr` / `TextInput` through the `lib.rs` re-export path where every other item uses its canonical module path, splits `std` imports into two blocks with a second `crate::` block, and has a function-local `use crate::input::keyboard::key::Key;`. `let scale = scale_factor;` rebinds a parameter to a shorter name for no other reason.

## Sibling APIs differ in name or shape

- [ ] `src/widgets/theme/widget_look/stateful_look.rs:206-211`, `src/widgets/theme/scrollbar.rs:28-36`, `src/widgets/theme/splitter.rs:14-22`, `src/widgets/theme/palette.rs` — the hover state is `hovered` (StatefulLook), `thumb_hover` (Scrollbar), `hover` (Splitter); the engaged state is `active`, `thumb_active`, `drag`. Palette uses `elem_hover` / `elem_active`.
- [ ] `src/widgets/theme/progress_bar.rs:13,17`, `src/widgets/theme/scrollbar.rs:17,22,28`, `src/widgets/theme/slider.rs:13,21`, `src/widgets/theme/separator.rs:25`, `src/widgets/theme/splitter.rs:14,18`, `src/widgets/theme/toggle.rs:200,211`, `src/widgets/theme/combo_box.rs:18,23`, `src/widgets/theme/context_menu/mod.rs:34,42`, `src/widgets/theme/context_menu/menu_item.rs:155`, `src/widgets/theme/tooltip.rs:23,43`, `src/widgets/theme/modal.rs:19` — the rail an indicator runs along is `track` in ProgressBar and Scrollbar, `rail` in Slider. Chrome is `panel` in ContextMenu and Tooltip, `card` in Modal. Cross-axis breadth is `width` (Scrollbar), `height` (ProgressBar), `thickness` (Separator rule; Splitter grab area, with the rule as `rule_thickness`), `rail_thickness` (Slider), `indicator_stroke` (Toggle), `arrow_stroke` (ComboBox); `Splitter.thickness` and `Separator.thickness` name different things. "Gap between two things" is `row_gap` in two bundles and `gap` in four. `ScrollbarTheme.radius` is a bare `f32` while every other bundle carries radius inside `Background.corners` or derives a pill from its height.
- [ ] `src/widgets/theme/toggle.rs:292-298`, `src/widgets/theme/widget_look/theme_slot.rs:296-298`, `src/widgets/theme/drag_value.rs:33` — `built(corner, box_size, indicator_inset, indicator, p)` takes three adjacent positional `f32`s; `theme_slot.rs` explains why `SlotDefaults` avoids exactly that shape. `from_chip(chip: ButtonTheme, text_edit: &TextEditTheme)` takes one bundle by value and the other by reference.
- [ ] `src/widgets/separator/mod.rs:81`, `src/widgets/spinner/mod.rs:83`, `src/widgets/progress_bar/mod.rs:41`, `src/widgets/splitter/mod.rs:105-106`, `src/widgets/slider/mod.rs:96-97`, `src/widgets/switch.rs:65` — theme scalars are floored ad hoc and inconsistently: `thickness.max(0.0)`, `diameter.max(1.0)`, `height.max(0.0)`, `thickness.max(1.0)` beside `rule_thickness.max(0.0)`, while `Slider`'s `knob_size` / `rail_thickness` and `Switch`'s `box_size` get no floor at all.
- [ ] `src/widgets/modal/mod.rs:38-42,71` vs `src/widgets/popup/mod.rs:88-103,143-158,190`, `src/widgets/tooltip/mod.rs:205` — `Modal::show` takes `FnOnce(&mut Ui)` and returns `ModalResponse { dismissed }`; `Popup::show` hands the body a `&PopupHandle` and reports `close_requested` separately. A dialog's own "OK" button has no way to close the modal through the widget, and `Modal`'s body returns no value where `Panel` / `Grid` return `InnerResponse<R>`. Every `Popup` constructor hard-codes gap `0.0` into `OverlayPosition::below/above/…(anchor, 0.0)`, which do take a gap, while `Tooltip` passes `theme.gap`.
- [ ] `src/widgets/combo_box/mod.rs:101,195`, `src/bin/showcase/pages/dialogs.rs:59` — `ComboBox` writes through `&mut usize` but returns the trigger's bare `Response`. The write happens on a `MenuItem` click inside the popup, not on the trigger, so unlike `Checkbox` / `Switch` / `RadioButton` the caller has no signal that the selection changed; `Slider` / `DragValue` / `TextEdit` return `changed`. The showcase re-reads the index instead.
- [ ] `src/widgets/tooltip/mod.rs:72-83,116-126`, `src/widgets/context_menu/menu_item.rs:75-77,82-84`, `src/widgets/context_menu/menu_separator.rs:32,38`, `src/widgets/separator/mod.rs:26,83-94` vs `src/widgets/splitter/mod.rs:41,138-139,181,202` — `Tooltip` carries two lifetimes so `text()` can rebind `'a` by rebuilding the whole struct field by field; every other labelled widget uses `label_setter!` over one lifetime. Naming splits four ways: `Tooltip::text`, `Button` / toggles `.label(...)`, `MenuItem::new(label)`, `Text::new(text)`. `MenuItem::enabled(e)` is `self.disabled(!e)` — the only inverted alias. `MenuSeparator` puts `#[track_caller]` on `show` rather than a constructor and, unlike `Separator`, implements no `Configure`. `Separator` encodes its axis as `horizontal: bool` and branches twice; `Splitter` holds an `Axis`.
- [ ] `src/renderer/backend/gpu_gradient_atlas.rs:130` vs `src/renderer/backend/mod.rs:581-680`, `:691-705` vs `:1037-1058`, `:494,505-507,527-529,860-862` — `GpuGradientAtlas::upload(&mut self, ctx: &GpuCtx)` takes a shared `GpuCtx` while every sibling uploader takes `&mut GpuCtx`. `run_dim_pass` takes a ready `color_view`; `run_overlay_pass` takes the `surface_tex` and creates a view itself, though `submit` already created a view of the same texture on the direct-present path. `ViewportPush { size: buffer.viewport_phys_f }` is constructed three times for one value.
- [ ] `src/renderer/backend/overlay_pass.rs:159` vs `:170`, `src/primitives/rect/mod.rs:173-178,218-227` — the outset uses `Rect::inflated(gap_px)` (a `f32`), the inset uses `Rect::deflated_by(Spacing::all(gap_px))`, because `Rect` has `inflated(f32)` and `deflated_by(Spacing)` but no `deflated(f32)`. `inflated` does not clamp; `deflated_by` clamps at zero. The pair cannot round-trip a per-side inset.
- [ ] `src/primitives/spacing/mod.rs:27-29,86-108` vs `src/primitives/corners/mod.rs:101-111`, `src/scene/shapes/lower.rs:200` — `Spacing::as_u64` exists for the layout hash; `Corners` has no equivalent, so the chrome hash spells it `bytemuck::cast(corners)`. `Spacing` implements `Add` and tuple `From<(X,Y)>` / `From<(L,T,R,B)>`; `Corners` implements neither and offers `From<Vec2>` / `From<Size>` instead — the two-value shorthand is a tuple on one type and a `Vec2` on the other.
- [ ] `src/shape/mod.rs:58-63,201-331`, `src/shape/text.rs:60-63`, `src/shape/icon.rs:84-87` — `Shape::rect`, `line`, `arc`, `circle`, `text` construct through `RectShape::new` / `CurveShape::new` / `TextShape::new` in the kind's own file; `Shape::triangle`, `polyline`, `shadow`, `image`, `icon`, `mesh` build the struct literal in `mod.rs`, so six kinds' defaults live away from their types. `TextShape::at(origin: Vec2)` shares its name with the `local_rect_shape!` `at(rect: impl Into<Rect>)` on every other rect-shaped kind but takes a different type with a different meaning. `IconShape::desaturate` is a hand-written setter while every other setter on the type goes through `shape_setters!`.
- [ ] `src/primitives/brush/gradient/gradient_builder.rs:41-49` vs `src/primitives/brush/gradient/mod.rs:98-108` — `GradientBuilder::with_spread` / `with_interp` are plain fns while `Gradient::with_spread` / `with_interp` are `const fn`, despite the builder doc stating the two are "spelled the same on purpose".
- [ ] `src/renderer/frontend/payload/draw_image_payload.rs:37-53`, `src/renderer/frontend/payload/push_clip_payload.rs:25-27`, `src/renderer/frontend/encoder/layer_ctx.rs:222,241,266,291,310,368`, `src/renderer/frontend/paint_sink/mod.rs:89-97` — `DrawImagePayload::image` is a six-positional-argument constructor that only assigns fields and has one caller; `PushClipPayload::rounded` is the same for two fields. Every other payload is built as a struct literal at the call site. `clip` / `pop_clip` sit beside `push_transform` / `pop_transform`, and the clip's payload is named `PushClipPayload`.
- [ ] `src/renderer/frontend/composer/session.rs:81-89,117-123,835-842` — `PackedQuad {phys_rect, urect, ..}` and `ScaledRect {phys, urect}` describe the same (physical rect, integer bounds) pair under different field names; `pack_quad` destructures a `ScaledRect` to populate a `PackedQuad` rather than embedding it.
- [ ] `src/scene/damage/walk.rs:113`, `src/scene/damage/mod.rs:138,280,380-391` — the same `DamageCounters` value is `counters` on `DamageEngine` and `probe` on `LayerWalk`. `compute` opens a `tracy::zone!()`; `compute_paint_only` — the per-frame path on every paint-only frame — does not.
- [ ] `src/text/key.rs:296-302` — `quantize_width` is the generic 1/64 quantizer and `quantize_metric` is defined in terms of it; the name says width.

## Comments narrate history, restate the line, or point at a renamed item

- [ ] Doc comments attached to the wrong item: `src/renderer/backend/mod.rs:1127-1138,1148` (the `begin_load_pass` paragraph sits in `batch_kind`'s block; `begin_load_pass` has no doc), `src/renderer/backend/backbuffer.rs:20-32,64` (`Backbuffer::new`'s doc describes `describes`, which is undocumented), `src/renderer/backend/text/mod.rs:160-178` (`flush`'s doc is on `shaper()`), `src/renderer/render_buffer/mod.rs:213-230` (`batches`' doc runs into `chains_equal`'s), `src/host/device_requirements.rs:83-104` (`met_by`'s doc is on `max_texture_dim`), `src/widgets/scroll/bars.rs:24-25` (`bar_reservation`'s doc begins mid-sentence), `src/renderer/backend/stencil.rs:65-69` (malformed doubled link), `src/renderer/backend/mod.rs:376-388` (two overlapping summaries), `src/layout/layout_scratch.rs:108` (doubled link target).
- [ ] Stale names and claims: `src/renderer/backend/mod.rs:183-185` (`CosmicMeasure` no longer exists), `mod.rs:411-419` (`via_backbuffer` / `surface_tex` became `Submission` fields), `src/renderer/backend/quad_pipeline.rs:1-3` ("viewport uniform"), `src/renderer/quad.rs:15-18` (`vertex_attr_array` in `QuadPipeline::new` is the `QUAD_INSTANCE_ATTRS` const), `src/renderer/backend/gpu_timings.rs:419-421` (`consume_slot` mentions a `ready` flag `Slot` lacks), `quad_pipeline.rs:264` ("upload_masks first" for `stage_masks`), `curve.wgsl:35-38` (cites `render_buffer.rs`), `mesh.wgsl:51` ("review A1"), `src/renderer/backend/gpu_gradient_atlas.rs:22-25` ("the shader assert above" — none in the file), `src/renderer/backend/icon/mod.rs:64-65` (stale fragment on `instances`), `src/renderer/backend/image_pipeline/render_target.rs:165-169` (`initialized` documented per-texture; it is per-entry, since `ensure` replaces the texture on resize without clearing it), `src/renderer/backend/text/mod.rs:22` and `encoder.rs:60` ("vs glyphon's 24", "Bumped from glyphon's 256").
- [ ] Stale names in the frontend and scene: `src/renderer/frontend/encoder/layer_ctx.rs:178,432` (`draw_triangle`, `draw_rect`, `draw_shadow`), `src/renderer/frontend/composer/clip_stack.rs:24` (`ComposeSession::push_clip`), `src/renderer/frontend/composer/text_grid/mod.rs:142` (`Composer::compose`), `src/renderer/frontend/payload/draw_quad_payload.rs:253` (`Shape::debug_assert_no_nan` — the gate is `has_nan` at `Shapes::add`), `src/renderer/frontend/payload/draw_polyline_payload.rs:43` (`PaintSink::draw_polyline` is on `PaintGate`), `src/renderer/frontend/composer/mod.rs:50-52,75-76` and `higher_kind.rs:1` (replay order and tracked tiers omit `PaintTier::Icon`), `src/scene/node/salt.rs:69` (`SeenIds::record` does not exist), `src/scene/tree/mod.rs:73-79` (SoA column list omits `extras`), `src/scene/record_store/mod.rs:34` (`clear` listed among "borrow internally" though it takes `&mut self`), `src/scene/node/mod.rs:53` ("five themable fields"; `clip` is a sixth), `src/scene/tree/tree_fingerprint.rs:40` and `tree/mod.rs:643-649` (a `Hasher::finish()` result named `paint_cardinality`, which reads as a count).
- [ ] Stale names in cascade, damage, layout, text, widgets, host: `src/scene/damage/mod.rs:50-54` (`counters.dirty` said gated `internals`; it is `TestOnly`), `damage/mod.rs:255-256` (`prev` said rolled via `entry()`; the walk deliberately uses `get` / `insert` / `remove`), `src/scene/damage/row_matcher.rs:215,244` (`[Self::begin]`; the phase is `reset_for`), `src/scene/cascade/counters.rs:11-12` (unused link definition), `src/scene/damage/counters.rs:1` ("Built on `BenchOnly`"; uses both), `src/scene/cascade/mod.rs:83` ("divided four ways", five bullets), `src/layout/grid/axis_scratch.rs:124,137` (`Self::reset` → `reset_for`; "`a.sizes`" no longer exists), `src/layout/counters.rs:80-85,98` (`LayoutEngine::replay_arranged` lives on `LayoutPass`; "`Self::begin_run`" → `begin_pass`), `src/text/cosmic/mod.rs:6,79,196,204`, `src/text/cosmic/cache_entry.rs:44` (`CosmicMeasure::end_frame` → `advance_to`), `src/text/system.rs:110-115`, `src/text/shaper.rs:281-282` ("two teardowns"; `TextSystem` has one), `src/widgets/toggle_chrome/mod.rs:36` (`toggle_row` → `record_row`), `src/widgets/checkbox/mod.rs:27-29` (`unchecked.pick` / `checked.pick`; `show` uses `slot.plan` and `ToggleTheme::pick` has no production caller in `widgets/`), `src/widgets/text.rs:25-26` (`Button` / `DragValue` "set `SingleLine`"; both use `Truncate`), `src/widgets/theme/scrollbar.rs:7-13` (`scroll::push_bar_nodes` does not exist; the gutter is reserved unconditionally for `Reserved`), `src/widgets/theme/text_style.rs:229-231,236-238` and `theme/mod.rs:129-132` ("Button / TextEdit … don't read this" contradicts `WidgetLook.text: None` inheriting `Theme::text`; `TextEdit::line_height_mult` builder does not exist), `src/widgets/theme/widget_look/mod.rs:27-29` (names states `pressed` / `focused`; both are `StatefulLook::active`), `src/widgets/theme/text_edit.rs:81-83` and `button.rs:135-137` (`pick` docs tell the caller to merge `state.disabled |= node.disabled`; `Widget::response` now does that), `src/frame_fixture/tokens.rs:85` ("the activity list is the only `FILL` one"; no caller passes `FILL`), `src/bin/showcase/support.rs:30-31` (cites `elem #343434`; the showcase installs `0x2b2e36`), `src/host/winit/gpu/mod.rs:101-110`.
- [ ] Change-history narration (commit-message content in code): `src/scene/damage/walk.rs:4-14`, `src/scene/cascade/paint.rs:136-138`, `src/scene/cascade/entry.rs:21-24`, `src/scene/layer.rs:47-49`, `src/scene/damage/row_matcher.rs:54-56`, `src/layout/fill_item/mod.rs:12-13`, `src/layout/intrinsic/mod.rs:82-92`, `src/layout/driver.rs:4-10`, `src/layout/stack/mod.rs:210-211`, `src/layout/wrapstack/mod.rs:9-11`, `src/layout/canvas/mod.rs:24-48`, `src/text/key.rs:52,57`, `src/text/cosmic/mod.rs:206-214,379-381`, `src/text/system.rs:100-105`, `src/text/mono.rs:56-59`, `src/ui/mod.rs:113-116,125-132,394-400,606-612`, `src/ui/frame_cycle.rs:121-127,361`, `src/ui/frame_runtime/mod.rs:216-220`, `src/input/input_state/mod.rs:270-273,410-413`, `src/input/policy.rs:7`, `src/input/keyboard/mod.rs:1-2`, `src/renderer/frontend/composer/session.rs:326-333`, `src/renderer/frontend/composer/text_grid/mod.rs:108-112,140-143`, `src/renderer/frontend/capture.rs:20-21`, `src/primitives/half_simd/mod.rs:168-173`, `src/primitives/widget_id/mod.rs:141-163`, `src/animation/spring.rs:15-21`, `src/primitives/rect/mod.rs:262-264`.
- [ ] The same rationale told in full three times: `src/primitives/background.rs:26-35`, `src/primitives/brush/mod.rs:20-28`, `src/animation/animatable.rs:21-30` (the "not `Copy` because of the `vmovups` ladder, ~35 % self-time" story); `src/scene/tree/recording_scratch.rs:1-4,9-15,31-35` (the "lives off `Tree` so downstream passes can't reach transient state" paragraph); `src/renderer/backend/raster_atlas/mod.rs:393-398,411-414` (the 256-alignment argument in the doc and again inline).
- [ ] Decorative section dividers: `src/scene/damage/walk.rs:154,171,204,238,408` (five `// ---- … ----`), `src/text/cosmic/mod.rs:468,609,697` (three inside one `impl`).
- [ ] Line narration: `src/renderer/backend/quad_pipeline.rs:411-421`, `src/renderer/backend/mesh_pipeline.rs:231-233`, `src/renderer/backend/curve_pipeline/mod.rs:194-199` (`vertex_attr_array!` tables with a trailing `// pos`, `// size` per lane plus a prose restatement above; the `offset_of!` guards already bind each lane), `src/ui/frame_cycle.rs:79-84,326-332` (one comment per statement in `pre_record`; a comment about payload clearing above an unrelated `debug_assert!`), `src/widgets/button.rs:33-40,86-88` and `src/widgets/widget.rs:108-110,116` (restating the `text_wrap` doc; hard-coding "seven widgets" and "the other ~50 sites"), `src/host/window_driver/mod.rs:200-204,485-487`, `src/host/winit/window.rs:152-162`, `src/host/winit/runtime.rs:108-111`, `src/host/offscreen.rs:1-38` (a 39-line module doc), `src/host/core.rs:75-81` (an `expect(dead_code, reason = ..)` whose string carries runs of embedded whitespace from a wrapped line), `src/renderer/frontend/composer/session.rs:293-294`.
